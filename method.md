# BEV 파이프라인 — 설계·실험 기록

> **이 파일은 모든 시행착오와 변경사항을 상세히 기록한다.**
> 채택된 결정뿐 아니라 **실측으로 기각된 가설과 그 근거**를 함께 남긴다. 기각 사유를 지우면
> 같은 오진을 반복한다 — 실제로 이 기록 안에서 "전역 상관을 품질 신호로 오인"하는 실수가
> 세 번 반복됐다 (X-05 · X-06 · X-07).

## 이 문서를 쓰는 법

**상태는 §0 에만 있다.** 본문 제목에는 상태를 적지 않는다 — 상태가 바뀔 때마다 문서를 뒤져야
하고, 현재 상황을 알려면 전체를 읽어야 하기 때문이다. **§0 을 읽고 멈출 수 있어야 한다.**

| 절 | 성격 | 갱신 방식 |
|---|---|---|
| §0 현재 상태 | 유일한 상태 소재지 | **덮어쓴다** |
| §1 현행 사양 | 코드가 지금 하는 일 | **덮어쓴다** |
| §2 결정 `D-nn` | 왜 그렇게 했나 | **추가만**. 제목은 고정 앵커 |
| §3 실험 `X-nn` | 무엇을 해봤나 (기각 포함) | **추가만**. 제목은 고정 앵커 |
| §4 방법론 | 재사용 도구·게이트 | 추가 |
| §5 대기 작업 | 실행 절차 상세 | 덮어쓴다 |
| 부록 | 결함 수정·폐기 설계 | 추가만 |

- 항목이 끝나면 **§0.2 에서 §0.3 으로 한 줄 옮긴다.** 본문은 손대지 않는다.
- 뒤집힌 결정도 본문을 지우지 않고 §0.3 에 `대체됨 → D-nn` 이라고만 적는다.
- 새 항목은 번호를 이어 붙인다. 결정은 실험 ID를, 실험은 결정 ID를 서로 참조한다.

---

# §0. 현재 상태 — **이 절만 읽으면 된다**

§0.3 에 있는 항목은 이미 닫혔다. **근거가 필요할 때만** ID 로 §2·§3 을 찾아 내려간다.

## 0.1 파이프라인

| 단계 | 산출물 | 상태 |
|---|---|---|
| 00 큐브맵 크롭 | `output/00_front/` 145,468장 | 완료 |
| 01 세그멘테이션 | `output/01_seg/` | 완료 |
| 02 단안 깊이 | `output/02_depth_f16/` 145,468장 (`disp_raw` float16) | 완료 |
| 03 BEV·차폐 | `output/03_bev3/` **36,339 / 36,367장** (건너뜀 28장 = 0.08%) | D-19·D-20 반영분. **D-21 반영 재실행 필요** (약 56분) |
| 03 코드 | `bev/`, `03_bev_shadow_gpu.ipynb` | 완료. 다음 변경 후보는 §0.2-B |
| 웹 배포 | 네 버전 동시 서빙 (`260811` · `260818` · `260818_2` · `260819`, 기본 `260819`) | 완료. 범례에서 버전을 바꾸면 지도 색상·DSI·BEV 이미지·지점 상세가 함께 바뀐다 → §5.1 |
| 웹 도로 선 | `web/public/data/roads.geojson` 4,566개 | 완료. 파노라마 점 잇기 → **도로구간 5m 포인트**(공식 형상, 0.2m 단순화) |

**최근 라운드 (D-19·D-20 / X-24·X-25)** — 탐색창 축소 + 게이트 교체 + `L_vis` = min.
`fit_far` 0.563 → 0.568, 근거리 0.820 → 0.803 (과적합 제거), 실행 시간 **90분 → 60분**
(`DYAW` 25개 → 5개라 FFT 가 1/5). 판정 불가 4.7% → 7.8%, 정지시거 플래그 2.92% → 7.57%.

**지금 가장 큰 문제는 정합이 아니라 지표 해상도다** — DSI 최소값 1.15 에 **52.0%** 가 묶여
하위 절반이 동일값이다(차폐율 0). 정합이 좋아져 허위 차폐가 사라진 결과라 원인 자체는
개선이지만, 노선 최적화에 엣지 가중치로 넣으면 도로망 절반이 같은 값을 받는다 → 근본 대응은 D-06.

**회전은 닫혔다 (D-21 / X-26·X-27).** 탐색을 없애고 메타데이터 + 상수 −2° 만 쓴다. 다만
**잔여 회전 σ ≈ 3.1° 는 실재한다**(측정 잡음 1.31°, 곡률·pitch·roll 로 설명되지 않음).
이 오차의 비용은 등급 변경 약 9~13% 이고, 원인 후보는 **면별 지면 픽셀 비대칭** → §0.2-B.

이전 라운드 요약은 **X-22**(전량 재실행 검증), 건너뛴 28장은 **X-23**.

## 0.2 열린 항목 — 지금 해야 할 것

**A. 배포 — 남은 것** (버전 전환·임계·크롭·지점 상세는 완료. 절차는 §5.1)

| ID | 할 일 | 어디를 | 왜 (한 줄) |
|---|---|---|---|
| — | `readme.md` 산출 수치·등급 서술 | §5.2 | 처리 36,339장·판정 불가 비율이 없고, 등급을 "전체 분포 3구간"이라 적어 D-11 과 어긋난다 |

**B. 정확도 (재실행이 드러낸 것 — 근거는 X-22)**

| ID | 할 일 | 어디를 | 왜 (한 줄) |
|---|---|---|---|
| **D-11** | **DSI 하위 해상도** — 등급 임계를 무엇으로 할지 확정 | `web/app/versions.ts` | 최빈값 1.15 에 **52.0%**. `p40 = p50 = 1.1500` 이라 tercile 불가. 가능한 구성은 사실상 `안전 = 차폐율 0`(52.0%) / 경계 1.3711 로 나머지 이등분(24%/24%) 하나뿐 |
| — | **잔여 회전 σ 3.1° 의 원인** | `calibrate_panorama` | 회전 *추정*은 닫혔지만(D-21) 오차는 남았다. edge 내(5.00°) > edge 간(4.16°) 이라 파노라마별 재구성 오차다. 후보: **면별 지면 픽셀 수 비대칭**이 공유 평면을 한쪽으로 끌어당김 — `n_ground_px` 좌우 비대칭과의 상관으로 검정 가능 (X-27) |
| — | **원거리 정합** | `refine_pose` | `fit_far` 평균 0.568, **< 0.5 가 39.6%**. 회전은 닫혔으므로 남은 결함은 **이동·스케일·평면** 쪽이다 |
| — | 게이트 임계 재검토 | `bev_core.ON_ROAD_MAX = 2.0` | 2m 가 최적이라는 근거는 없다. 전량 within-edge 이탈도 차: **2m +0.028(5.3%) / 3m +0.049(3.5%) / 5m +0.076(1.9%)** — 올릴수록 판별력이 세진다 (X-25) |
| — | `seam_max` 를 판정에 넣을지 결정 | `calib_valid` | 현재 **기록만** 한다. `seam_max ≥ 0.3` 이 6.6%, 그중 `seam_rms` 로는 안 걸리는 숨은 불량이 5.4% (X-18·X-22 §6) |
| **G6** | 절대 스케일 검증 (파노라마 간 IoU + scale 스캔) | §4.3 | 거리 스케일이 GIS 리그 높이 가정 하나에만 의존. 유일한 외부 검증 수단 |

**C. 확장 (재실행 불필요 — `_rtheta.npz` 로 오프라인 가능)**

| ID | 할 일 | 왜 (한 줄) |
|---|---|---|
| **D-06** | 도메인 재검토 (중심선 1-D vs 폴리곤 면적 2-D) | `road_occlusion` 은 `r(θ)` + GIS 만 쓴다. 36,339장 전량 오프라인 비교 가능 |
| **D-13** | emergence 레이어 | 미착수. `r(θ)` 인접 불연속 = 골목 입구·차 사이 틈 |
| — | 어린이보호구역·도로 종단경사 미반영 | D-08. 데이터 없음. 기록만 |

## 0.3 닫힌 항목 색인 — 결론만

**결정 (전부 코드 반영 완료)**

| ID | 결론 |
|---|---|
| D-01 | 02 는 `disp_raw` float16 저장. 8bit 양자화 바닥 제거 (고유값 249 → 7,158) |
| D-02 | `R_MAX = 40 m`. D_stop 최대 36.9m 를 덮는 최대값 |
| D-03 | `H_BAND = (1.0, 3.0) m`. 클래스별 2군 분류는 불필요 (mountain·hill 만 예외) |
| D-04 | 면별 `(a,b)` 8개 + 공유 지면평면 동시 적합. `h` 는 GIS 에서 파노라마별 고정 |
| D-05 | 레이캐스팅 → 극좌표 히스토그램. 판정은 "누적 3표 도달 반경". dilate 해킹 소멸 |
| D-07 | 연속 위험도(도로 차폐율)와 이진 경보(`L_vis < D_stop`)를 분리. 곱하지 않는다 |
| D-08 | 도로 등급 → 30/50/60 km/h → `D_stop` 13.4/27.9/36.9 m |
| D-09 | 모든 차량을 occluder 로 센다. 구조물만/차량포함 두 값을 병기 |
| D-10 | BEV 에 GIS 폴리곤 실제 모양 + 사진이 본 도로면을 겹쳐 그린다. 사각지대는 도로 위/밖으로 분리 |
| D-11 | 웹 등급 임계는 의미 기반. **버전마다 자기 분포에서 다시 뽑는다** (`web/app/versions.ts`) |
| D-12 | `TILT_MAX_DEG` 8.0. 수평 강제 클램프가 만들던 원거리 높이 드리프트 제거 |
| D-14 | `faces300 < 2` 는 `dsi_refined`/`grade` 를 `None` 으로. `dsi_raw` 는 진단용으로 남긴다 |
| D-15 | 차량 뒤 도로는 **분모에서 뺀다**(가려짐도 보임도 아님). `road_unknown_frac` 로 기록 |
| D-16 | pose 탐색 한계에 붙은 파라미터가 2개 이상이면 정합 미수렴 → 판정 불가 |
| D-17 | `sight_flag` 는 구간 판정. `[L_vis_veh, L_vis]` 가 `D_stop` 한쪽에 있으면 확정, 걸치면 `None` |
| D-18 | 출력을 가로 3패널(1748×727)로. 세로 649×2187 은 한 화면에 안 들어와 비교가 안 됐다 |
| D-19 | pose 게이트 교체. `clipped ≥ 2`(판별력 0) 폐기 → **카메라가 실폭도로 폴리곤 밖 2m 초과**면 판정 불가 |
| D-20 | `L_vis` = **min(전방, 후방)**. 평균은 막힌 방향을 가린다. 링크는 양방향 주행이 가능하다 |
| D-21 | **회전 탐색 폐기.** 메타데이터 방위각 + 상수 −2° 만 쓴다. 탐색이 잔여 회전을 오히려 키웠다 |

**실험 (전부 종료)**

| ID | 결론 |
|---|---|
| X-01 | 구 IPM 은 최하단 픽셀의 51.8%만 실제 지면접점 → 재설계 근거 |
| X-02 | 4면 정규화를 합쳐도 affine ambiguity 는 안 없어진다 → 외부 기하 제약 채택 |
| X-03 | `h` 미지수 동차계에 쓰레기 해가 있고 특이값으로 검출 불가 → `h` 고정 |
| X-04 | "비대향 쌍이 없으면 평면 미결정" 우려는 기우 (std ≤ 0.43°) → 가드 불필요 |
| X-05 | **`fit_score` 편향설 철회.** within-edge 초과 ≈ 0. 전역 상관 −0.35 는 전부 도로폭 교란 |
| X-06 | 봉우리 "뾰족함"도 폭에 공정하지 않다 → **정합 신뢰도 게이트는 포기** |
| X-07 | 탐색창 확대는 93%가 무변화, 한계 사례는 오히려 손해가 더 잦다 → 기각 |
| X-08 | `tilt_clamped` 편향의 원인은 수평 강제 클램프. 초과분이 10m 밖에 몰림 → D-12 |
| X-09 | `few_ground` 은 `faces300=0`(−0.320)과 `=1`(+0.292) 두 집단이 반대 방향 → D-14 |
| X-10 | sidewalk 포함 시 hold-out 63.5% 악화 → **실폭도로 폴리곤에 인도는 없다** |
| X-11 | DSI 최소값에 47.8%가 묶여 `quantile(1/3)` 이 그 값과 일치 → D-11 |
| X-12 | 6m 고정 리본이 "사진과 다르다"의 주원인. 정합 레이어와 지표 레이어도 불일치 → D-10 |
| X-13 | 평면 투영 거리 민감도: 40m 에서 픽셀당 **5.00m (12.5%)** → 정합은 20m 이내만 |
| X-14 | `H_BAND (0.4, 2.2)` 는 연석·화단을 차단물로 잡았다 → D-03 |
| X-15 | 도로망 `unary_union` 노드화로 ±40m 확보 실패 54.1% → **1.0%** |
| X-16 | `refine_pose` FFT 화: 1,043 → 86 ms, 완전탐색과 **6/6 일치** |
| X-17 | 차량 뒤 낙관 편향은 도로 길이의 6.6%p. 단 (c)"가려짐으로 계수"는 이미 `dsi_veh` 와 동일값 → D-15 |
| X-18 | 이음매 중앙값이 최악을 가린다(최악/중앙 배수 2.09, 중앙 통과·최악 0.10 초과가 23.4%) |
| X-19 | 정합 신뢰도: 양방향(IoU·역커버리지)은 판별력 없음. 전/후방 분리가 1.8배 강하나 폭 교란 잔존 → 기록만 |
| X-20 | `R_MIN_EGO` 는 면별. 자차는 front/back 에만 크고 left/right 가 지우던 건 진짜 근접 벽이었다 |
| X-21 | 정합 히스토그램을 점 개수 → **셀 점유**로. 근거리 14:1 우세가 회전을 떠 있게 만들고 있었다 |
| X-22 | **전량 재실행 검증.** 회전 오차 68% 해소·보류 7.2%→1.1%·차폐율 −14%. 남은 결함은 원거리 정합과 `\|dxy\|` 한계 도달 |
| X-23 | 건너뛴 28장은 **파노라마 좌표가 중심선에서 35~123m 이탈**한 지점. 로직 실패가 아니라 입력 결손 → 조치 불필요 |
| X-24 | 방위각은 `camera_angle[1]` 이 맞고(도로축 대비 0.6°, `heading` 은 38.1°), **0/180 모호성 없음**(28,114쌍 중 0건), 실측 σ **0.43°**. 창 ±24° 는 56σ 였다 → 창 축소 + `YAW_OFFSET −2°` |
| X-25 | 게이트 후보 within-edge 전수 검정. 옛 `clipped≥2` 는 **판별력 0**(대조군과 동일), `폴리곤 밖 >2m` 만 유효 → D-19 |
| X-26 | 회전을 기하로 계산하려는 세 시도(도로면 PCA·방사 프로파일·회랑 벽면) **전부 기각**. 최고 신뢰 구간에서도 패배 |
| X-27 | **평가 기준 교체**. `fit_far` 는 회전 신호/잡음 0.010 이라 1° 급을 못 본다 → **잔여 회전(도)** 도입. 이 자로 재니 탐색이 상수보다 나쁘다 → D-21 |

**게이트 판정** (상세 §4.3): G1 통과 · G2 통과 · **G4 기준 폐기** · G3·G5·G7 무의미해짐 · **G6 미실행**

---

# §1. 현행 사양

## 1.1 데이터 흐름

```
00_front/*_{left,front,right,back}.jpg   (256x256 큐브맵 4면, yaw -90/0/90/180)
        │
        ├─ 01_seg/*_masks.npz + _meta.json   클래스별 마스크
        └─ 02_depth_f16/*_depth.npz          disp_raw (float16, affine 불변)
                │
      [03] calibrate_panorama : 면별 (a_d,b_d) 8개 + 공유 지면평면 n 동시 적합   → D-04
                │
      ├── face_points  : inv_z = a·disp + b → metric 점군 → 높이대역 필터        → D-03
      │        └─ occupancy → radial_profile → r(θ) 720칸                       → D-05
      │                 ├─ l_vis / sight_flag                                    → D-07
      │                 └─ road_occlusion(GIS 도로 중심선) → dsi_refined         → D-06
      │
      └── ground_bev  : 광선 × 적합평면 (깊이 미사용) → refine_pose(폴리곤 정합)
                                                          → (dyaw, dx, dy)
             ※ 정합 히스토그램은 점 개수가 아니라 셀 점유(0/1). 원근 때문에 점의 93%가
               10m 안에 몰려 개수로 세면 회전이 근거리에 압도당한다 (X-21)
```

**깊이가 쓰이는 곳**: 보정 · 차단물 점군 · `r(θ)` · L_vis · A_shadow — 즉 측정의 전부.
**깊이를 안 쓰는 곳**: `ground_bev`(도로면). 지면 픽셀에는 평면이 근사가 아니라 정의이고,
그 평면을 지면 픽셀로 적합했으므로 깊이를 쓰면 순환이 된다.

## 1.2 핵심 수식

```
보정   1/Z_d = a_d·disp_d + b_d                      (Z = 면 좌표계 z-depth)
평면   n·X = h, |n|=1, n_y>0  ⇒  1/Z = (1/h)·nᵀ(R_d r),  r = (x_n, y_n, 1)

지면행   a_d·disp_i + b_d = (1/h)·nᵀ(R_d r_i)         (road|sidewalk, 지평선 아래)
이음매행 a_p·disp_p(v,255) + b_p = a_q·disp_q(v,0) + b_q
         (cx=127.5 라 u=255 와 u=0 이 주점 대칭 → range 변환 인자 |r| 이 정확히 상쇄)

분리 최소제곱 + 단위구 제약 (닫힌 형식)
    Q = YᵀW²Y − (YᵀW²A)(AᵀW²A)⁻¹(AᵀW²Y) + λ·diag(1,0,1)
    n = Q 의 최소 고유벡터 (n_y>0),  x = (AᵀW²A)⁻¹AᵀW²Y·n
    Huber IRLS 3회 (k = 1.345·1.4826·MAD).  h 는 GIS CSV 에서 파노라마별 고정.

점군   Z = 1/inv_z;  P = R_d(Z·r);  height = h − nᵀP
       rng = hypot(P.x, P.z);  theta = atan2(P.x, P.z)

r(θ)   bi = ⌊(θ mod 2π)/2π·N_BINS⌋;  ri = ⌊rng/RADIAL_CELL⌋
       H = bincount(bi·n_cells + ri);  C = cumsum(H, axis=radius)
       r(θ) = RADIAL_CELL·(argmax(C ≥ MIN_VOTES) + 0.5),  미달 시 R_MAX

지표   road_occluded_frac = Σ(가려진 링크 길이) / Σ(도달 가능 링크 길이)
       dsi_refined = (1 + road_occluded_frac) · (1 + V_heavy)
       sight_flag  = 1{ L_vis < D_stop(도로 등급) }   (차량 차폐 과다 시 None)
       A_shadow = Σ (Δθ/2)·(R_MAX² − r(θ)²)           (star-shaped 라 해석적으로 정확)
```

## 1.3 파라미터

```python
R_MAX = 40.0;  CANVAS = 240;  GRID_RES = 2*R_MAX/CANVAS    # 0.33333 m/px  → D-02
CENTER = (120, 120)
FX = FY = 128.0;  CX = CY = 127.5      # 256px 큐브맵 면의 올바른 주점
                                       # (00 은 128.0 을 저장 → 디스크 K 를 읽지 않는다)
N_BINS = 720;  RADIAL_CELL = 0.5;  MIN_VOTES = 3           # → D-05
H_BAND = (1.0, 3.0)                    # 시선 차단 대역 (m)  → D-03, X-14
R_MIN_EGO = {"left": 1.0, "front": 3.0, "right": 1.0, "back": 3.0}   # 면별 → X-20
GRAD_REJECT_REL = 0.15                 # 인접 픽셀 |Δ(1/Z)|/(1/Z) 상한 (flying pixel)
TILT_PRIOR = 3e-3;  TILT_MAX_DEG = 8.0 # 4.0 이었다 → D-12, X-08
GROUND_PX_MIN = 300;  FACES_MIN_VALID = 2                  # 보정 식별성 → D-14, X-09
ROAD_UNKNOWN_MAX = 0.5                 # 차량 뒤 도로가 이 비율 초과면 판정 불가 → D-15
ON_ROAD_MAX = 2.0                      # 카메라가 실폭도로 밖 이만큼 넘으면 판정 불가 → D-19
SEAM_W = 3.0;  SKY_Q = 0.2;  SUB = 3
BACKGROUND_CLASS_IDS = {16, 68}        # mountain, hill
CAM_HEIGHT_DEFAULT = 2.5               # GIS 실측: 2.5m 76% / 3.0m 12% / 2.0m 12%
YAW_OFFSET = -2.0                      # 사진 도로면 ↔ GIS 의 계통 회전 → X-24, X-26
DYAW = array([YAW_OFFSET])             # **회전은 탐색하지 않는다** → D-21, X-27
DXY = arange(-3, 3.1, 1)               # 옛 ±8 은 ≤20m 에 과적합 (근거리↑ 원거리↓)
PEN_YAW = 0.03;  PEN_XY = 0.04;  POSE_RES = 0.5;  POSE_MARGIN = 16.0
PEN_YAW_REF, PEN_XY_REF = 24.0, 8.0    # 페널티 척도는 창과 분리 (아래 주의)
EPSG = 5179
SPEED_BY_CLASS = {"2": 60.0, "3": 50.0, "4": 30.0}         # → D-08
VEH_BLOCK_MAX = 0.5;  V_HEAVY = 0.15
```

`GRAD_REJECT` 는 반드시 **스케일 무관 상대 기준**이어야 한다 — 8bit `depth_norm` 과 float16
`disp_raw` 는 절대 스케일이 달라 절대 임계값은 재실행 후 조용히 깨진다.

**페널티 정규화를 창으로 하면 안 된다.** 옛 코드는 `PEN_YAW*(dyaw/DYAW.max())²` 였는데, 이러면
창을 줄일 때 도·미터당 페널티가 함께 세진다(24→4 이면 36배). 창 변경의 순효과를 측정할 수
없게 되므로 `PEN_*_REF` 상수로 분리했다. 회전 페널티는 `YAW_OFFSET` 중심으로 재야 한다 —
안 그러면 기본값 −2° 에 이미 페널티가 붙는다. 부록 A 의 `PEN_XY` 결함과 같은 결합이다.

## 1.4 출력 스키마 (`output/03_bev3/{pano}_dsi.json`)

| 그룹 | 필드 |
|---|---|
| 지표 | `dsi_refined` `grade` `dsi_veh` `grade_veh` (**판정 불가면 `null`**) · `dsi_raw`(진단용, 항상 값) |
| 판정 가능 여부 | `valid` = `calib_valid`(D-14) ∧ `road_known_enough`(D-15) ∧ `on_mapped_road`(D-19) · `cam_off_road_m` |
| 도로 차폐 | `road_occluded_frac`(분모=아는 도로) `road_occluded_m` `road_span_m` `road_known_m` `road_unknown_frac` `road_occluded_frac_veh` |
| 지시함수 | `sight_flag` (True/False/**null**) `sight_flag_veh` `l_vis_margin_m` `road_class` `speed_limit_kmh` `d_stopping_m` |
| 가시거리 | `l_vis_m`(= **min(전,후)**, D-20) `l_vis_fwd_m` `l_vis_bwd_m` `l_vis_veh_m` `l_vis_censored` |
| 차량 | `veh_blocked_fwd` `veh_blocked_bwd` (기록용. D-17 이후 판정 기준이 아니다) |
| 원판(참고) | `a_shadow_disk_m2` `a_total_disk_m2` `r_max_m` `h_band_m` `v_heavy` `cam_height_m` |
| 보정 | `calib{a, b, pitch_deg, roll_deg, seam_rms_rel, seam_max, seam_worst, seam_per{4개}, n_ground_px, faces300, status}` |
| 정합 | `pose{fit_score, dyaw_deg, dx_m, dy_m, clipped, fit_far, fit_fwd, fit_bwd}` (`clipped` 는 D-19 이후 **기록 전용**) |
| 품질 | `coverage{n_points, n_raw, starved_bins, empty_bins}` `method_version` |

별도 파일 `{pano}_rtheta.npz` — `r_occ`/`r_veh` 720칸 float16 (장당 838B, 전량 약 29MB).
`road_occlusion` 은 `r_theta` + GIS 만 쓰므로 **도메인 실험(D-06)을 재실행 없이** 할 수 있다.

`bev/` 패키지: `bev_core.py`(수식·지표) · `bev_render_worker.py`(3패널 렌더, spawn 워커) ·
`road_prep.py`(도로망·도로면 전처리, 전체 1회).

---

# §2. 결정 기록

각 항목: **결정 / 근거 / 실측**. 상태는 여기에 적지 않는다 — §0 에서 본다.

## D-01. 02 는 원출력 disparity 를 float16 으로 저장한다
**결정**: `hf_pipeline(task="depth-estimation")` → `AutoModelForDepthEstimation` 직접 호출.
정규화·뒤집기 없이 `predicted_depth` 를 `disp_raw`(float16)로 저장. 출력 폴더 `output/02_depth_f16/`.

**근거**: 파이프라인 후처리가 `predicted_depth` 를 min-max 정규화 후 **uint8 PIL 로 반환**한다.
저장돼 있던 `depth_norm` 은 고유값이 249개(전부 k/255)뿐이었고, 이 양자화가 `dZ = Z²/288` 이라는
거리 오차 바닥을 만들었다 — **30m 에서 3.1m, 60m 에서 12.5m**. 30m~무한대를 10단계, 60m~무한대를
5단계로만 구분하는 셈이라 파이프라인 전체 원거리 정확도의 상한이었다.

**왜 `depth_norm` 이 아니라 원출력인가**: 원거리는 disparity→0 으로 수렴하므로 float16 의
상대정밀도(2⁻¹¹)를 그대로 받는다. `depth_norm`(원거리가 1.0 근처)으로 저장하면 float16 간격
0.00098 에 걸려 이점이 절반으로 준다. 03 이 어차피 `1/Z = a·disp + b` 로 스케일을 다시 푸는
이상 중간 정규화는 정보만 버린다.

**부호 근거**: HF 후처리는 `(predicted_depth − min)/(max − min)` 이라 **뒤집기가 없다**. 기존
노트북이 그 출력을 disparity(큰 값 = 가까움)로 취급해 정상 동작해왔으므로 `predicted_depth` 도
같은 방향이다. 실측 `disp_min` 은 음수가 흔한데(예 −10.6) affine 불변량이니 정상이고, 03 은
`inv_z ≤ 1/R_MAX` 로 걸러낸다.

**실측**: 고유값 **249 → 7,158**. 저장 1.6GB → 9.3GB, GPU 재추론 약 3시간, 03 실행시간
+1.6ms/pano(약 +1%).

**하위호환**: `bev_core.load_face` 가 `disp_raw`(신규)/`depth_norm`(구)을 자동 인식.
`_depth_meta.json` 의 `disp_min`/`disp_max` 로 옛 `depth_norm` 을 복원할 수 있다.
**단 구 `output/02_depth/` 는 이후 삭제되어 실제 롤백 경로는 없다.**

## D-02. 분석 반경 R_MAX = 40 m
**근거**: D_stop 최대값(36.9m)을 덮으면서 양자화 상대오차 ≤14% 를 유지하는 최대값.
R=60 대비 A_shadow 동적범위가 0.31 → 0.69 로 넓어진다.

## D-03. 시선 차단 대역 H_BAND = (1.0, 3.0) m
**결정**: 지면 위 1.0~3.0m 를 점유한 점만 시선 차단물로 센다. 클래스별 2군 분류(지면접합 vs
캐노피)는 **하지 않는다**.

**근거**: 지면 시선을 막느냐는 클래스가 아니라 대역 점유 여부로 결정된다. 벽의 base 를 가린
차량은 그 자체가 거의 같은 거리의 occluder 라 답이 바뀌지 않는다. 나무는 줄기가 대역에 있고
캐노피는 없어 자동 처리된다. 예외는 **mountain/hill(16, 68)뿐** — 수 km 밖이라 상위 양자화
단계에 몰려 35~40m 유령 벽이 된다.

**주의**: 대역 두께는 Z=10m 23행 → 20m 12행 → **40m 6행**으로 얇아진다. 높이 조건수 자체는
거리보다 훨씬 좋다(40m·1.3m 에서 δheight 0.17m).

초기값 (0.4, 2.2) 는 오류였다 → X-14.

## D-04. 파노라마 단위 공동 metric 보정 (h 고정)
**결정**: 면별 `(a_d, b_d)` 8개와 공유 지면평면 `n` 을 지면행 + 이음매행으로 **동시** 적합.
카메라 높이 `h` 는 GIS CSV 에서 파노라마별로 **고정**한다. 수식은 §1.2.

**h 고정이 핵심**: h 를 미지수로 두면 계가 동차가 되어 `a≈0, b_d≈const` 라는 쓰레기 해가
참해보다 낮은 잔차를 갖는다(실제 반례 `point_83520_...`: pitch −89.55°, Z≈−2.5m). 그리고
**특이값 비율로 검출되지 않는다**(sv비 19.4 로 정상처럼 보임). h 고정으로 비동차가 되면
이 함정이 사라진다. → X-03

**가드 (구현 현황 주의)**

| 계획한 가드 | 실제 구현 |
|---|---|
| `\|tilt\| > 4°` → `n=(0,1,0)` 고정 재적합 | **구현됨** (→ D-12 로 변경 예정) |
| `a_d ≤ 0` → 해당 면 기각 | **라벨(`bad_scale`)만 설정, 계산 불변** |
| 비대향 쌍 없음 → `n=(0,1,0)` | **라벨(`few_ground`)만 설정, 계산 불변** |
| 4면 모두 지면 <300px → 전역 prior | 미구현 |

**실측 효과** (이음매 상대 불일치 중앙값): 면별 독립 **0.085** → 이음매 결합 **0.053**
→ +pitch/roll **0.038**. 비용 8ms/pano. 전체 실행 seam 잔차 중앙 **0.047**.

## D-05. 레이캐스팅 → 방사 프로파일
**결정**: 격자 래스터 레이캐스팅을 폐기하고 극좌표 히스토그램 + 누적합으로 `r(θ)` 를 구한다.
판정은 **"누적 투표가 `MIN_VOTES`(3)에 처음 도달하는 반경 셀"**.

**근거**: 백분위수는 스케일 의존적이다 — 벽은 수천 점, 기둥은 여덟 점이다. 고정 투표수는
물체 크기와 무관하게 "무언가 있다"를 판정한다. 부수 효과로 격자 이산화가 사라져 **레이 누출
방지용 3×3 dilate 해킹이 불필요**해졌다. 240×240 격자는 이제 시각화 전용이다.

## D-06. A_shadow 도메인 = GIS 도로 중심선 가시성
**결정**: 카메라에서 **도로를 따라**(Dijkstra) 40m 이내인 중심선 링크를 도메인으로 하고,
`가려진 링크 길이 / 전체 링크 길이` 를 잰다. 폭을 쓰지 않는 1-D 지표.

**근거**: 원판 도메인(`A_total = π·60²`)은 건물 뒤 영역이 대부분인데 그건 도로변 어디서
찍어도 무조건 안 보인다. 그래서 비율이 "얼마나 둘러싸였나"라는 거의 상수가 되어 **DSI 분산의
1.8%만 설명**했고(중앙값 0.877), 정작 중요한 '못 보는 도로'가 그 안에 묻혔다.
2-D 코리도를 쓰지 않은 이유는 GIS 좌표 오차(카메라↔최근접 도로정점 median 2.5m)가 중앙 도로
반폭(4.5m)과 같은 급이라, 마스크가 자기 폭만 한 오차로 놓이기 때문이다.

**실측**

| 도메인 | p10 | p50 | p90 | 폭 |
|---|---|---|---|---|
| 기존 R=60 원판 | 0.649 | 0.877 | 0.958 | 0.309 |
| R=40 원판 | 0.407 | 0.719 | 0.877 | 0.469 |
| **도로 차폐율** (표본) | **0.000** | **0.158** | **0.485** | **0.485** |
| **도로 차폐율** (전체 36,337장) | 0.000 | 0.049 | 0.476 | 0.476 |

**도로망 생성**: `강릉시내.shp`(도로구간 원본)를 `unary_union` 으로 노드화 후 5m 조밀화
(`bev/road_prep.py`, 98,486 노드 / 99,386 링크). → X-15

**재검토 사유 (미결)**: 폴리곤 면적 2-D 도메인으로 가면 ① 그림과 지표가 같은 대상이 되고
② `refine_pose` 가 맞추는 레이어(폴리곤)와 지표가 쓰는 레이어(중심선)의 불일치가 사라진다.
그러나 ③ "도로를 따라 도달 가능"이라는 개념이 사라져 건물 뒤 다른 도로까지 도메인에 들어가
원판의 포화 문제로 부분 회귀하고 ④ 폴리곤 속성이 `OPERT_DE/RW_SN/SIG_CD` 뿐이라 차도·주차장
구분이 없다(인도는 없음 — X-10 에서 확인). 판단에는 `r_occ` 저장 후 오프라인 비교가 필요하다(§5.1).

## D-07. DSI 구성 = 연속 위험도와 이진 경보를 분리
```
dsi_refined = (1 + road_occluded_frac) · (1 + V_heavy)     연속 위험도 (순위/등급)
sight_flag  = 1{ L_vis < D_stop(도로 등급) }                이진 레드 플래그 (원래 의도)
보조        = l_vis_margin_m, road_class, speed_limit_kmh, d_stopping_m
```

**`D_stop/L_vis` 를 곱에서 뺀 이유**: (1) 실측상 DSI 분산의 87%를 그 항이 차지해 "인지 불가
구간의 위험도"라는 본래 의도와 반대로 작동했다. (2) 도로 차폐율과 L_vis 는 둘 다 "도로를 볼 수
있는가"를 재므로 곱하면 같은 위험을 두 번 센다 — 전체 실행에서 `spearman = −0.622` 로 확인됐다.

**부수 효과 — 검열 문제 소멸**: L_vis 는 R_MAX=40m 에서 63.9%가 상한에 걸려 순위 정보가 없다.
그러나 `D_stop` 최대값(36.9m) < `R_MAX`(40m) 이므로 **검열됐다는 것 자체가 `L_vis > D_stop`
확정**을 뜻한다. 지시함수 판정에 검열이 무해해진다.

**`sight_flag = None` (판정 불가)**: 전방/후방 콘의 절반 이상이 차량에 가려지면
(`veh_blocked > VEH_BLOCK_MAX`) L_vis 중앙값 자체를 믿을 수 없으므로 False 가 아니라 None 을
둔다. 전체 실행에서 **7.2%**.

## D-08. D_stop = 도로 등급 → 제한속도 → 정지거리
| `ROA_CLS_SE` | 유형 | 폭 중앙 | 제한속도 | D_stop | 근거 |
|---|---|---|---|---|---|
| `4` (67.1%) | 길 | 6m | 30 km/h | 13.4m | 안전속도 5030 이면도로 |
| `3` (28.8%) | 로 | 20m | 50 km/h | 27.9m | 안전속도 5030 도시부 일반도로 |
| `2` (4.1%) | 대로 | 35m | 60 km/h | 36.9m | 간선도로 상향 지정 |

- 등급 판별은 도로명주소법 시행령 제6조(대로 40m/8차로, 로 12~40m, 길 그 외)와 데이터가 일치
  (폭 중앙 35/20/6m, 도로명 끝글자). `(edge_id, point_id)` 조인 **결측 0건**.
- 제한속도는 도로교통법 시행규칙(2019-04-17 개정, 2021-04-17 전국 시행). 이면도로에 '설계속도'
  기준이 없는 이유는 설계가 아니라 운영(제한속도)으로 규율되기 때문이다.
- `D_stop` 은 반응 1s·μ=0.7 (자율주행 기준). **설계기준 정지시거(반응 2.5s)는 50km/h 에서 63m
  로 `R_MAX` 를 넘어 채택할 수 없다.**

**밝혀둘 것**
1. 대로의 60km/h 는 **부분적으로 계산 가능성이 선택에 영향을 줬다** — 70km/h 면 D_stop=47m 로
   `R_MAX` 를 넘어 판정 불가가 된다. 대안은 등급 `2`(4.1%)만 R 을 늘리는 것이나 양자화 오차가 커진다.
2. 검증 경로: 표준노드링크 `MAX_SPD` 와 겹치는 `2`·`3` 등급(약 33%)에서 배정을 대조할 수 있다.

**한계**: 어린이보호구역(30km/h 강제)은 데이터가 없어 미반영(대부분 이면도로라 이미 30km/h).
도로 종단경사 미반영(`land_altitude` 가 있으나 별도 검증 필요).

## D-09. 진행 중인 차량도 전부 occluder 로 센다
**결정**: 동행 차량 제외 로직(구 `split_vehicles_by_angle`)을 쓰지 않는다. 대신 `include_vehicles`
플래그로 **구조물만**(장소의 속성 = 지도 등급용)과 **차량 포함**(촬영 순간의 실측)을 둘 다 낸다.

**근거**: 앞 차량을 장애물로 보지 않더라도 그 차량 앞이 어떤 상황인지 모르는 건 마찬가지다.
주어진 자료(스트리트뷰 이미지)에 최대한 정직해야 한다. 동행 차량 제외는 방법 자체가 부정확할
뿐 아니라, 동행 차량이 꼭 전·후방에만 있으라는 법도 없다.

**단** 촬영일 교통량이 그 도로의 구조적 위험도는 아니므로, 지도 등급(`dsi_refined`)은
구조물 기준을 쓰고 차량 포함분은 `dsi_veh` 로 병기한다.

## D-10. BEV 그림은 두 도로 출처를 함께 그린다
**결정**: 고정 6m 리본 대신 **실폭도로 폴리곤의 실제 모양**을 그리고, 그 위에 **파노라마 자신이
본 도로면**(`ground_bev`)을 겹친다. 중심선은 "측정 대상" 표시선(폭 1.5m)으로 남긴다.
사각지대는 **도로 위(진한 주황, 계수됨)**와 **도로 밖(연한 크림, 맥락)**으로 나눠 칠한다.

**읽는 법**
- 회색만 있고 파랑 없음 → **가려서 못 본 도로** (측정하려는 것)
- 파랑이 회색 밖으로 나감 → **정합 오차** (믿으면 안 되는 것)

**근거와 검증**: → X-12

## D-11. 웹 등급 임계는 기계적 tercile 이 아니라 의미 기반
```
T = (1.1501, 1.4375)   →   Safe 47.8% / Caution 25.7% / High-risk 26.5%
Safe    = 40m 안에 가려진 도로가 없음 (road_occluded_frac == 0)
경계2   = 나머지의 DSI 중앙값
```

**근거**: DSI 최소값 1.1500 에 **47.8%가 묶여 있어** `quantile(1/3)` 이 정확히 1.1500 이다.
웹 코드는 `dsi < T[0] → Safe` 이므로 그대로 쓰면 **Safe 가 0%** 가 되고 47.8%가 전부 Caution 이
된다. → X-11

## D-12. `TILT_MAX_DEG` 4.0 → 8.0
**결정**: 지면평면 기울기 클램프 임계를 8° 로 올린다. 8° 초과는 지금처럼 `n=(0,1,0)` 강제.

**근거**: 현행 4° 클램프가 `tilt_clamped` 집단(5.5%)에 **+0.230** 의 계통적 과대 차폐를 만든다.
클램핑 전 기울기는 p50 5.0° 로 대부분 4°를 겨우 넘긴 값인데 0°로 강제하니 과잉 교정이다.
8° 초과에서는 seam 잔차가 오히려 나빠져(0.114 vs 0.086) 클램프가 제 역할을 한다. → X-08

**효과**: 잔여 편향 +0.230 → **+0.080**, 여전히 클램프되는 비율 100% → 9.8%.

## D-14. 보정이 성립하지 않은 파노라마는 값을 내지 않는다

**결정**: `faces300`(지면 300px 이상인 면 개수)이 0 또는 1 이면 `dsi_refined` 를 등급에 쓰지
않고 **판정 불가**로 표시한다. `faces300` 는 `calib.n_ground_px` 에서 사후 산출 가능하지만,
렌더와 웹 등급에도 반영되어야 하므로 필드로 명시한다.

**근거**: → X-09. 두 집단이 반대 방향으로 틀린다.
- `faces300 = 0` (0.5%): 이음매+prior 만으로 적합 → 점이 거의 `inv_z > 1/R_MAX` 를 통과하지
  못해 **모든 방향이 "트임"으로 보고**된다. within-edge 초과 **−0.320**. 지금 이들은
  DSI 1.15 = 최하위 등급 "Safe" 로 지도에 찍힌다 — **보정 실패를 안전하다고 표시하는 것**이다.
- `faces300 = 1` (1.8%): 한 면만 지면에 고정되고 나머지 세 면이 이음매를 타고 연쇄 결정 →
  스케일 오차 누적 → 허위 차단물. within-edge 초과 **+0.292**.

**범위**: 합쳐 2.3% 뿐이다. `faces300` 2~3 인 29.8% 는 −0.052 / +0.021 로 멀쩡하므로 건드리지
않는다 — "지면이 덜 보인다"가 곧 "값이 나쁘다"는 아니다.

**선례**: `sight_flag` 를 차량 차폐 과다 시 `None` 으로 두는 것(D-07)과 같은 원칙이다.
틀린 값을 내느니 판정하지 않는다.

## D-15. 차량 뒤 도로는 분모에서 뺀다

**결정**: `road_occlusion(..., r_veh=)` 이 구조물에 가려지지 않았으면서 차량이 먼저 막는 링크를
**'모름'으로 분류해 분모에서 제외**한다. `road_occluded_frac` 의 의미가 "**아는 도로** 중 안
보이는 비율"이 되고, 얼마나 몰랐는지는 `road_unknown_frac` 으로 따로 남긴다.

**근거**: 구조물만으로 재면 차량이 가린 방향은 그 뒤의 구조물이 애초에 안 보여 `r_occ` 가
커지고, 결과적으로 그 도로가 "보인다"고 계수된다. 실측 도로 길이의 **평균 6.6%p**가 그렇다.

**크기 주의 — (b)는 수치 교정이 아니라 정의 교정이다** (→ X-17):
분모에서 빼는 것이라 값은 평균 +0.007 오를 뿐이고 순위상관은 0.997 이다. 웹이 순위로 등급을
매기므로 등급은 사실상 그대로다. "가려진 것으로 계수"(+0.050)는 이미 `dsi_veh` 와 같은 값이다.

**선례**: `sight_flag=None`(D-07), `faces300<2`(D-14)와 같은 원칙 — 모르면 판정하지 않는다.

## D-16. pose 가 탐색 한계에 붙으면 판정하지 않는다

**결정**: `dyaw`·`dx`·`dy` 중 탐색 범위 경계(±24°, ±8m)에 붙은 것이 **2개 이상**이면
정합 미수렴으로 보고 값을 내지 않는다(`pose_converged=False`).

**근거**: 한계에 붙었다는 것은 최적점이 창 밖이거나 애초에 뚜렷한 최적점이 없다는 뜻이다.
X-07 에서 창을 넓혀도 93%는 해가 안 움직이고 한계 사례는 오히려 악화가 더 잦음을 확인했으므로,
**창을 넓히는 대신 표시**한다. 같은 edge 이웃 대비 초과 차폐:

| 한계 도달 | 비율 | 초과 차폐 |
|---|---|---|
| 1개 | 5.5% | +0.078 |
| **2개** | **1.0%** | **+0.108** |
| **3개** | **0.2%** | **+0.187** |

임계를 2개로 두면 **1.2%만 제외**하고 +0.121 편향을 걷어낸다. 1개까지 포함하면 6.7%가 빠지는데
+0.078 이라 비용 대비 효과가 떨어진다.

## D-17. `sight_flag` 는 구간 판정

```
진짜 구조적 가시거리 ∈ [L_vis_veh, L_vis]
  L_vis < D_stop      -> True   (상한조차 미달, 차량과 무관)
  L_vis_veh > D_stop  -> False  (하한조차 초과, 차량이 있어도 확보)
  그 외                -> None   (구간이 D_stop 을 걸친다)
```

**근거**: `r_veh <= r_occ` 가 항상 성립하므로(차단물을 더하면 가시거리는 짧아질 뿐. 실측 위반
**0건**) 차량 뒤의 불확실성에는 경계가 있다. 하한은 "벽이라도 최소한 차량만큼은 멀다",
상한은 "차량이 없었다면 구조물까지 보였다". 구간이 `D_stop` 한쪽에 통째로 놓이면 차량 뒤를
몰라도 결론이 바뀌지 않는다.

**효과**: 판정 불가 **7.17% → 1.22%**. 기존 `None` 2,604장 중 2,130장이 확정 False,
114장이 확정 True 로 갈린다. 반대로 기존이 근거 없이 단정하던 **85장**은 올바르게 불가가 된다.

이전 규칙(`veh_blocked > 0.5` 면 무조건 `None`)은 **불확실성의 방향을 구분하지 않은 과보수**였다.
`VEH_BLOCK_MAX` 는 더 이상 판정에 쓰이지 않는다(`veh_blocked_*` 는 기록만).

## D-18. 출력은 가로 3패널 (1748×727)

**결정**: 세로 3단(649×2187) → 가로 3패널. 제목은 3행(ID / 결론 / 품질).
`bev_core.suptitle_for()` 한 곳에서 만든다.

**근거**: 종횡비 1:3.4 는 한 화면에 안 들어와 세 패널을 비교할 수 없었다. 가로로 두면
좌→우로 "무엇이 있나 / 구조물만 사각 / 차량 포함 사각"을 나란히 읽는다. 부수 효과로 제목 폭이
그림 폭을 넘지 않아 **G7 절단 문제가 소멸**한다.

**수치 중복 제거**: `road occluded`·`DSI` 가 제목과 두 패널에 세 번 나오던 것을 제목 한 번으로.
패널에는 거기서만 알 수 있는 값만 둔다(패널2 = L_vis 전/후방 분해, 패널3 = 차량 포함 수치).

**웹 영향**: 크롭이 행 기준에서 열 기준으로 바뀐다. G7 은 폐기한다(§4.3).

## D-19. 정합 게이트를 "탐색 한계"에서 "도로 지도 위에 있는가"로 바꾼다

**결정**: `pose_clipped ≥ 2 → 판정 불가`(D-16)를 폐기하고, **카메라가 실폭도로 폴리곤에서 2m 넘게
벗어나면** 판정 불가로 둔다. `pose.clipped` 는 기록만 한다.

**근거 1 — 옛 기준은 판별력이 0이었다.** within-edge 전수 검정(34,663장 / edge 3,496개)에서
이탈도 차 **+0.005 ± 0.002**, 무작위 50% 대조군 −0.002 와 사실상 같다. 29.3% 를 걸러내면서
아무것도 못 집는다. 새 창에서는 적중률이 2.2% → 29.7% 로 뛰므로 그대로 두면 판정 불가가
30% 를 넘는다. (X-25)

**근거 2 — 게이트의 목적은 "정합이 나쁜 장 거르기"가 아니다.** D-14 의 논리가 원형이다: 보정
실패가 "모든 방향 트임" = **안전으로 위장**하는 것을 막는 장치다. 위험을 못 맞히는 것보다
위험을 낮게 말하는 것이 비싸다. 그러니 게이트가 답해야 할 질문은 하나다 — *카메라가 실제로
본 것과 GIS 도로가 같은 자리에 놓였는가*. `clipped` 는 **추정기가 어떻게 움직였는지**를 잴 뿐
결과가 맞았는지를 재지 않는다.

**근거 3 — 폭 교란이 없다.** X-05 가 `fit_score` 게이트를 기각한 이유는 좁은 골목이 구조적으로
낮은 포함률을 갖고 동시에 실제로 더 가려지기 때문이다. 폴리곤 밖 거리는 다르다: 좁은 골목은
폴리곤이 가늘어도 카메라가 그 **안**에 있다. 정합 전 겹침이 0.1 미만인 집단은 폴리곤 안 비율이
**9.5%**(정상군 91.4%)로, 좁은 도로가 아니라 도로 지도에 없는 곳이다.

**실측 (전량)**: 적중률 5.3%, 이탈도 차 +0.028 ± 0.006 (t=4.3), 초과차폐 +0.102.
판정 불가 4.7% → 7.8% (보정 2.3% / 도로미상 0.4% / 지도밖 5.6%).

**미확정**: 임계 2.0m 는 검증 표본에서 고른 값이고 최적이라는 근거가 없다. 3m 는 +0.049(3.5%),
5m 는 +0.076(1.9%) 로 올릴수록 판별력이 세진다 → §0.2-B.

**부수 효과**: 이 판정은 GIS 만으로 계산되므로 이미지 처리 **전에** 걸 수 있다. 전처리 단계로
옮기면 5.3% 의 연산을 아예 건너뛸 수 있다.

## D-20. `L_vis` 는 전방·후방의 최소값이다

**결정**: `l_vis()` 가 `(f, b, 0.5*(f+b))` 대신 `(f, b, min(f, b))` 를 반환한다.

**근거**: 셔틀은 링크를 양방향 중 어느 쪽으로도 지난다. 한 방향이라도 `D_stop` 을 못 채우면 그
주행 방향에서는 멈출 수 없다. 정지시거 지시함수는 순위용 연속 지표가 아니라 **즉시 조치용
이진 경보**이므로(D-07) 두 방향 중 나쁜 쪽이 기준이다.

**옛 근거가 둘 다 무효가 됐다.** ① `l_vis` docstring 의 "촬영차가 어느 쪽을 보고 지나갔는지에
대표 위험도가 의존하면 안 된다" — 이건 `fwd` 단독을 배제하는 논거지 평균을 지지하는 논거가
아니다. `min` 도 앞뒤 교환에 불변이다. ② "0/180 모호성이 있으니 평균이 안전하다" — X-24 에서
모호성 자체가 없음이 확인됐다(28,114쌍 중 뒤집힘 0건).

**실측 (36,339장)**: 플래그 True 2.92% → **7.57%**, 판정 불가 1.07% → 2.01%.
뒤집히는 1,578장(4.34%)은 전방 13.2m / 후방 28.2m 로 **차이 중앙 23.0m** — 잡음이 아니라 한쪽이
실제로 막힌 경우다. 평균 24.9m 가 "안전"이라 말하던 것을 min 11.8m 가 잡는다(`D_stop` 13.4m).
등급별로 이면도로 8.29% / 일반도로 6.43% / 대로 3.76%.

**D-17 전제 보존**: `r_veh ≤ r_occ` 가 방향별로 성립하므로 min 을 취해도 `L_vis_veh ≤ L_vis` 다.
36,339장 전량에서 위반 0건으로 확인했다.

## D-21. 회전은 탐색하지 않고 상수로 고정한다

**결정**: `DYAW = [YAW_OFFSET]`. 메타데이터 방위각 `camera_angle[1]` 에 상수 −2° 만 더해 쓰고,
파노라마별 회전 탐색을 없앤다. `pose_clipped` 는 이동만 센다(0~2).

**근거 — 새 자로 재니 탐색이 해롭다.** `fit_far` 는 회전 1° 당 0.0026 만 움직이는데 파노라마 간
표준편차가 0.2568 이라 신호/잡음이 0.010 이다(X-27). 회전에 민감한 **잔여 회전**(GIS 도로 축 대비
사진 도로 중심선의 기울기, 도 단위)으로 다시 재면 결과가 뒤집힌다.

| 회전 결정 | \|잔여회전\| 중앙 (주행 기준) | >2° 비율 | `fit_far` |
|---|---|---|---|
| 탐색 (구) | 1.96° | 49.3% | 0.670 |
| **고정 −2°** | **1.77°** | **44.0%** | 0.668 |

짝지은 Wilcoxon **p=0.0014** 로 유의하고, `fit_far` 손해 −0.0023 은 **유의하지 않다**(p=0.2).

**결정적 증거 — 탐색이 벗어날수록 대칭적으로 나빠진다.**

| 탐색이 고른 `dyaw` | n | 탐색 \|잔여회전\| | 고정 −2° |
|---|---|---|---|
| −6° | 148 | 2.64° | **1.97°** |
| −2° | 238 | 1.31° | 1.31° *(같은 답, 정합성 확인)* |
| +2° | 145 | 2.77° | **2.00°** |

탐색이 −2° 에 머문 장이 가장 정확하고 양쪽으로 벗어난 장이 가장 부정확하다. **겹침 최대화가
이동·형상 오차를 회전 자유도로 흡수하고 있었다.**

**앞선 판단 정정**: X-26 에서 "고정 −2° 가 `fit_far` 로 −0.0043 진다"를 근거로 상수를 배제했는데,
그 차이는 유의성을 확인하지 않은 값이었다. 유의하지 않다.

**부수 효과**: `refine_pose` 의 FFT 가 5회 → 1회. 장당 93ms, 36,339장 환산 약 56분.
차폐율 분포는 불변(0.160 동일).

**주의 — `pose_clipped` 를 함께 고쳐야 한다.** 옛 식은 `abs(dyaw − YAW_OFFSET) >= DYAW.max() −
YAW_OFFSET` 인데 `DYAW` 가 한 원소가 되면 우변이 0 이라 **모든 장이 회전 한계 도달**로 계산된다.

## D-13. C3 emergence 레이어
커밋 `9c0f98c` 로 삭제된 `test/03_bev_shadow.ipynb` 의 `detect_emergence` / `h_leak`
(인접 레이 거리 불연속 = 골목 입구·차 사이 틈을 근접 가중). 새 `r(θ)` 프로파일 위에서는
훨씬 깨끗하고 저렴하다. D-06 의 도로 도메인과 교차시키면(그림자가 도로와 만나는 가장자리만
계수) 바로 "인지 불가 구간에서의 유입 위험"이 된다.

---

# §3. 실험 기록

각 항목: **가설 / 방법 / 결과 / 판정**.

## X-01. 구 03 IPM 방식의 결함 실측
**대상**: 열별 지면접점 IPM (각 열 최하단 blocker 픽셀 1개만 역투영).

| 결함 | 실측 |
|---|---|
| 최하단 픽셀이 실제 지면 접점인 열 | **51.8%** 뿐 (나머지는 폐색 경계) |
| base 가 v≤132 라 Z≥71m 로 계산되어 폐기 | 24.5% |
| `HOOD_MASK_FRAC=0.25` 가 v≥192 를 덮어 **Z<5m 전 구간** 누락 | blocker 열의 21.6%, 전부 Z=5.06m 로 재배치 → L_vis 과대 → DSI 과소 |
| 4면 depth 독립 affine → 면 경계 상대 불일치 | 중앙 **8.5%** (20m 에서 1.7m) |
| `CAM_HEIGHT=2.5` 고정 vs GIS 실제 | 2.5m 76% / 3.0m 12% / 2.0m 12% → 4장 중 1장이 ±20% 오차 |
| pitch/roll=0 가정 vs 적합값 | roll 중앙 **+0.75°**(p90 +2.5°) 계통적. 2° roll 은 면 경계에서 20m 기준 ±28% |

## X-02. 4면 정규화를 합쳐서 ambiguity 를 없앨 수 있는가
1. 저장된 4장은 각각 이미 정확히 `[0,1]` 을 채운다(25파일 실측 전부 min=0.0, max=1.0).
   이어붙여 재정규화하면 **항등 연산**이다.
2. 더 근본적으로 Depth Anything V2 는 **추론 1회당** affine 불변 출력을 낸다. 4장을 따로
   추론한 순간 미지의 (scale, shift) 4쌍이 생기며 이는 모델의 성질이지 후처리 탓이 아니다.
   원출력 float16 저장으로 바꿔도 **면별 8개 미지수는 남는다.**
3. 등장방형 원본 1회 추론은 ambiguity 를 1개로 줄이지만 DA-V2 가 원근 학습이라 왜곡에서
   품질이 무너진다. (`method.md:130` 이 근거를 "정규화 기준이 뒤섞여서"로 적어둔 것은 부정확 —
   실제 이유는 기하 왜곡. **문서 수정 대상**.)
4. 00 에서 겹치게 재렌더(90° 면을 45° 간격 8장)하면 강한 제약이 생기지만 01·02 를 각각 2배
   (약 6h씩) 재실행해야 한다.

**채택**: ambiguity 를 외부 기하 제약(공유 지면평면 + 이음매)으로 푼다 → D-04. 재실행 0시간.

## X-03. h 를 미지수로 둔 동차계
최초 정식화는 11-DOF 동차 SVD 였다. 반례 `point_83520_pano_1U_6uh4lrekdjVKEHLvgGw` 에서
pitch −89.55°, Z≈−2.5m 인 쓰레기 해가 참해보다 낮은 잔차를 가졌고 **특이값 비율(19.4)로는
정상처럼 보였다.** 또한 단일 평면 1개 면만으로는 영공간이 2차원이다. → h 고정(D-04).

## X-04. 식별성 — 비대향 쌍이 없으면 평면이 미결정인가
**가설**: 지면이 보이는 면이 90° 떨어진 쌍으로 2개 이상 필요하며 front+back(대향)은 무효.

**방법**: left+right 지면이 0인 사례(20장 중 3장)에서 지면 픽셀을 무작위 절반씩 나눠 10회 재적합.

**결과**: **std(pitch) 0.01~0.25°, std(roll) 0.03~0.43°** — G2 기준(1.0°)을 여유 있게 통과.

**판정**: 이음매 제약과 지면 패치의 2차원 확장이 법선을 충분히 고정한다. 추가 가드 불필요.

## X-05. `fit_score` 가 편향 지표인가
**가설**: pose 정합이 실패한 파노라마가 차폐율을 부풀린다.

**최초 근거 (전역 상관)**
```
spearman(fit_score, 차폐율) = -0.353
fit_score <0.5 구간 차폐율 중앙 0.187  vs  >0.85 구간 0.037   (5배)
```

**반증 (within-edge, §4.1)**: 초과 차폐 **−0.017 / +0.008** — 대조군(+0.002)과 구별 불가.

**진단**: 전부 도로폭·위치 교란. `fit_score` 는 "사진의 도로 점이 GIS 폴리곤 안에 드는 비율"
이라는 **한쪽 방향 포함률**이라, 폴리곤이 넓으면 몇 미터 어긋나도 공짜로 1.0 이 나온다.
```
fit_score 중앙   대로 0.998 | 로 0.990 | 길 0.847
```

**판정**: 낮은 `fit_score` 는 "좁은 도로"를 뜻할 뿐 값이 틀렸다는 뜻이 아니다. 게이트 근거 없음.
`< 0.7` 로 자르면 23%가 사라지고 그중 **95.2%가 이면도로**이며 남은 데이터의 "차폐 0" 비율이
올라간다 — 편향이 아니라 **신호를 지우는 것**이다.

## X-06. 상관면 "봉우리 뾰족함"이 폭에 공정한 신뢰도인가
**가설**: `fit_score`(봉우리 **높이**)가 폭의 대리변수라면 봉우리 **모양**은 폭과 무관할 것이다.
회전교차로처럼 폴리곤이 등방적이면 상관면이 평평해 "어디로 밀어도 같다"를 직접 잡을 수 있다.

**방법**: 600장, 네 지표를 측정하고 ① 도로등급 상관(0에 가까울수록 공정) ② hold-out 상관(§4.2).

| 지표 | 도로등급 상관 | hold-out 상관 |
|---|---|---|
| `peak` (= 현 fit_score) | −0.507 | +0.869 |
| `sharp_contrast` (봉우리 − 창 평균) | +0.527 | −0.315 |
| `sharp_null` (봉우리 − 무보정) | +0.458 | −0.439 |
| `sharp_flat` (봉우리 ±0.02 면적비) | −0.564 | +0.604 |

**판정**: 넷 다 등급 상관 0.46~0.56 으로 공정하지 않다. 좁은 골목은 폴리곤이 가늘어 상관면이
태생적으로 뾰족하고 넓은 도로는 평평하다 — **높이든 모양이든 결국 폭을 잰다.** 등급 내부에서도
`sharp_contrast` 는 로 −0.71 / 길 +0.15 로 부호가 뒤집힌다.
X-05 와 합쳐: **정합 신뢰도 게이트는 만들 수 없고 만들 필요도 없다.**

## X-07. pose 탐색창 확대
**가설**: 5.6%가 `DXY` 한계(±8m)에 붙어 있으니 실제 오차가 창보다 크다.

**방법**: hold-out(근거리 ≤10m 로 정합 → 중거리 10~20m 로 평가), 600장 짝지은 비교.
`PEN_XY` 는 창으로 정규화돼 있어(`PEN_XY*((dx²+dy²)/DXY.max()²)`) 창을 넓히면 미터당 페널티가
약해진다 — 같은 기울기를 유지하려면 `(새창/8)²` 배 해야 한다.

| 설정 | hold-out | 한계 도달 | 현행 대비 (짝지음) |
|---|---|---|---|
| ±8 pen.04 (현행) | 0.6552 | 5.35% | — |
| ±12 pen.04 | 0.6576 | 4.68% | +0.0024 (개선 13.2% / 악화 10.9%) |
| ±12 pen.09 | 0.6576 | 2.34% | +0.0024 (개선 2.8% / 악화 2.8%) |
| ±16 pen.16 | 0.6622 | 1.00% | +0.0070 (개선 3.7% / 악화 2.8%) |

**판정**: **A→D 에서 93%는 해가 전혀 움직이지 않는다.** 한계에 붙었던 파노라마에서는 오히려
**개선 25.0% / 악화 31.2%** 로 손해가 더 잦다. 즉 ±8 에 붙은 것은 "정답이 8m 밖이라서"가 아니라
**상관면에 뚜렷한 답이 없어 가장자리로 흘러간 것**이며, 창을 넓히면 더 멀리 흐를 뿐이다.

## X-08. `tilt_clamped` — 클램프 자체가 원인
**가설**: `|tilt| > 4°` 일 때 `n=(0,1,0)` 수평 강제가 높이 오차를 만든다. 지면이 실제로
기울어져 있으면 오차가 거리에 비례해 커진다 — 40m 에서 4° 는 2.8m 로 시선대역 폭 전체다.
원거리의 트인 도로가 통째로 차단물이 된다.

**방법**: 혼재 edge 260개(파노라마 1,035장, `tilt_clamped` 549장)에서 세 정책을 비교.
기준선은 같은 edge 의 `ok` 이웃(0.285).

**클램핑 전 기울기**: p50 **5.0°**, p90 7.7°, max 76.1°.
`4~6° 74.7% | 6~10° 19.3% | 10~20° 5.1% | 20°+ 0.9%` — 대부분 4°를 겨우 넘긴 값.

| 정책 | 차폐율 | 초과 | seam |
|---|---|---|---|
| `current` 수평 강제 | 0.497 | **+0.230** | 0.094 |
| `edge4` 4° 경계로 축소 | 0.469 | +0.201 | 0.078 |
| `noclamp` 클램프 없음 | 0.328 | **+0.060** | 0.085 |

**거리 구간별 초과 — 높이 드리프트의 직접 증거**

| 구간 | 0–10m | 10–20m | 20–30m | 30–40m |
|---|---|---|---|---|
| ok 이웃 | 0.043 | 0.227 | 0.402 | 0.435 |
| current | 0.077 | 0.489 | 0.699 | 0.727 |
| **차이** | **+0.034** | **+0.263** | **+0.297** | **+0.293** |

근거리 초과는 작고 10m 밖에서 급증 — 가설대로다.

**임계 결정**: `noclamp` 잔여 편향이 4~6° +0.040, 6~8° +0.081, 8~10° +0.244 로 8°에서 꺾인다.
seam 잔차도 8° 이하에서는 `noclamp` 가 낫지만(0.079 vs 0.094) 초과에서는 나빠진다(0.114 vs 0.086).

| `TILT_MAX_DEG` | 잔여 초과 | 여전히 클램프 |
|---|---|---|
| 4° (현행) | +0.230 | 100% |
| 6° | +0.100 | 25.3% |
| **8°** | **+0.080** | **9.8%** |
| 10° | +0.076 | 6.0% |
| ∞ | +0.060 | 0% |

**판정**: `TILT_MAX_DEG` 8.0 채택 → D-12.

## X-09. `few_ground` — 두 집단이 정반대로 틀리고 있었다
라벨 하나로 묶여 +0.222 로 보였으나, 지면이 300px 이상 보이는 **면 개수**로 나누니 갈렸다.

| `faces300` | 비율 | within-edge 초과 차폐 |
|---|---|---|
| **0** | 0.5% | **−0.320** (과소) |
| **1** | 1.8% | **+0.292** (과대) |
| 2 | 12.3% | −0.052 |
| 3 | 17.5% | +0.021 |
| *(대조) 4면 중 무작위 절반* | *34%* | *−0.009* |

**메커니즘**
- `faces300 = 0`: 이음매와 prior 만으로 적합 → 실측 `seam_residual = 0.000`, 차폐율 중앙 `0.000`.
  점이 거의 `inv_z > 1/R_MAX` 를 통과하지 못해 **모든 방향이 "트임"으로 보고된다.** 지금 이들은
  DSI 1.15 = "Safe" 로 지도에 찍힌다 — **보정이 성립하지 않은 것을 안전하다고 표시하고 있다.**
- `faces300 = 1`: 한 면만 지면에 고정되고 나머지 세 면이 이음매를 타고 연쇄 결정 → 링을 돌며
  스케일 오차 누적 → 허위 차단물 (seam 0.060 vs ok 0.042).

**판정**: 문제는 2.3%(`faces300 ∈ {0,1}`)에만 있다. `faces300` 2~3 인 29.8%는 멀쩡하다 —
지면이 덜 보인다고 값이 나빠지는 게 아니다.
**조치**: `faces300 = 0` → 판정 불가 표시(현재 "Safe" 는 명백히 잘못). `= 1` → 신뢰 불가 표시.

## X-10. `ground_bev` 에 sidewalk 포함
**가설**: 도로면 회랑 형상이 완전해져 정합이 안정될 것이다.

**결과**: `hold-out  road 0.6552 → road+sidewalk 0.5806` (개선 7.5% / **악화 63.5%**).

**판정**: **실폭도로 폴리곤에 인도가 포함돼 있지 않다.** 속성만으로는 알 수 없던 사실이
실측으로 확정됐다. D-06 재검토 시 폴리곤 면적은 **차도만**이라는 전제가 필요하다.

## X-11. 웹 tercile 이 기계적으로 불가능
```
DSI 최소값 = 1.1500,  이 값에 묶인 비율 = 47.8%
quantile(1/3) = 1.1500   ← 동점 덩어리 경계와 정확히 일치
```
웹은 `dsi < T[0] → Safe` 이므로 `T[0]=1.15` 면 **Safe 가 0%** 가 되고 47.8%가 전부 Caution 이 된다.

## X-12. 시각화 — GIS 의존이 "정직하지 않게" 보였던 이유
**출발점**: 회전교차로·교차로에서 BEV 도로 모양이 파노라마와 어긋나 결과를 신뢰하기 어렵다.

**코드에서 확인된 원인 셋**
1. **그리는 도로가 실제 도로가 아니었다.** `road_grid(..., width=6.0)` 은 중심선을 따라 고정
   6m 띠를 그린다. 폭 35m 대로도 회전교차로도 똑같은 6m 라 **정합이 완벽해도 사진과 다르다.**
2. **맞춘 레이어와 쓰는 레이어가 다르다.** `refine_pose` 는 실폭도로 **폴리곤**에 등록하는데
   `road_occlusion`/`road_grid` 는 도로구간 **중심선**을 쓴다. 별개 데이터셋이다.
3. **사진의 도로 증거를 계산해놓고 버렸다.** `ground_bev(key="road")` 는 pose 정합에만 쓰이고
   그림에 그려지지 않았다.

**구현** (`bev_core.surface_grid` / `bev_canvas`, `bev_render_worker` 의 `road_mask`)
`refine_pose` 가 이미 만든 폴리곤 래스터를 BEV 좌표계로 최근접 재표본한다 — 폴리곤을 다시
점-in-폴리곤 판정하면 파노라마당 57,600 회가 더 드는데 그 정보는 이미 `mask` 안에 있다.

**검증**
- 역변환 왕복 오차 < 1e-6 (모든 bearing/offset)
- 캔버스에서 사진 도로점이 폴리곤 위에 놓인 비율과 `fit_score` 의 잔차 **−0.010~0.000**
  (회전 18°, 이동 8m 사례 포함)
- `ground_bev` 를 40m 로 계산해 20m 로 자른 집합이 기존 20m 호출과 **동일**
  (`hypot(x,z) ≥ |z|` 이므로) → **정합 결과 불변**
- JPEG 649×2187 유지 (G7), 추가 비용 canvas 4~13ms/pano

**그러나 사진만으로 되돌아가는 것은 선택지가 아니다.** A_shadow 는 "못 보는 도로"를 재는데
못 보는 도로는 정의상 사진에 없다. 260811 방식이 정직했던 이유와 A_shadow 를 못 쟀던 이유가
같다. 문제는 GIS 사용 여부가 아니라 **배치가 맞았는지 검사·표시하지 않았다는 것**이다.

## X-13. 평면 투영의 거리 민감도
`ground_bev` 는 광선 × 평면 교점이라 부각이 작아질수록 거리가 폭발한다 (h=2.5m).

| 거리 | 부각 | 1픽셀당 거리변화 | 상대 |
|---|---|---|---|
| 5 m | 26.6° | 0.08 m | 1.6% |
| 10 m | 14.0° | 0.31 m | 3.1% |
| 20 m | 7.1° | 1.25 m | 6.2% |
| 40 m | 3.6° | **5.00 m** | **12.5%** |

원거리 도로면이 방사형으로 늘어져 보이는 이유이며, 정합에 20m 이내만 쓰는 근거다.

## X-14. `H_BAND` (0.4, 2.2)
계획서 초기값으로는 연석·화단(0~1m)이 차단물로 잡혀 **트인 교차로가 L_vis 10.2m 로 보고**됐다
(`vis_2_blocked.png`). 운전자 시선 높이 ≈1.1m 기준으로 (1.0, 3.0) 으로 올렸다.

## X-15. 도로망 노드화
`강릉시내.shp` 는 위상적으로 노드화돼 있지 않다(T자 교차가 선 중간에 닿음). 끝점 근접 조인은
±40m 확보 실패가 **54.1%** 였다. `unary_union` 노드화 + 5m 조밀화로 **1.0%** 가 됐다
(원본선 끝점일치 54.1% → 점+5m 조인 1.2% → 노드화 1.0%).

## X-16. `refine_pose` FFT 화
완전탐색 25×17×17 = 7,225 회로 파노라마당 **1,043ms**(전체의 89%)를 먹었다. `fftconvolve` 는
회전 하나당 모든 이동량의 상관을 한 번에 주므로 **86ms** 가 됐고 완전탐색과 **6/6 정확히 일치**했다.
전체 파이프라인 1,043 → 164 ms/pano.

---

## X-17. 차량 뒤 도로를 어떻게 다룰 것인가

**측정**: 구조물만 기준에서 "보인다"고 세지만 실제로는 차량 뒤라 모르는 도로가 **평균 6.6%p**
(p90 0.242). 그런 구간이 5%를 넘는 파노라마가 34.9%.

**세 방식 비교** (같은 표본 295장, 판정 가능분)

| 방식 | 평균 | p50 | 0인 비율 | 현행 대비 | spearman |
|---|---|---|---|---|---|
| (a) 현행 — 차량 무시, 분모 전체 | 0.125 | 0.000 | 52.2% | — | — |
| **(b) 채택 — 분모에서 제외** | 0.132 | 0.000 | 52.2% | **+0.0074** | **0.997** |
| (c) 가려진 것으로 계수 | 0.174 | 0.125 | 36.9% | +0.0497 | 0.853 |
| (참고) `dsi_veh` | 0.174 | 0.125 | 36.9% | — | — |

**두 가지가 드러났다.**
1. **(c)는 `dsi_veh` 와 수치가 완전히 같다** — "차량 뒤를 가려진 것으로 센다"와 "차량을 장애물로
   센다"는 같은 말이다. 새 선택지가 아니라 이미 병기하던 값이었다.
2. **(b)의 수치 효과는 미미하다** (+0.007, 순위상관 0.997). 분모에서 빼는 것이라 값이 5% 오를
   뿐이다. 처음에 "지표값의 48% 규모"라고 표현한 것은 **(c)의 변화폭을 (b)의 편향 크기로 잘못
   제시한 것**이다.

**판정**: (b)를 채택하되 **정의를 정직하게 만드는 변경**으로 자리매김한다(D-15). 낙관 편향의
상한은 이미 `dsi_veh` 로 관리되고 있었다.

## X-18. 이음매 잔차는 4개인데 중앙값 하나만 저장하고 있었다

`SEAMS` 는 닫힌 고리라 4쌍인데 `seam_residual` 이 중앙값만 반환했다. 계획서 Part D-a 가
"이음매별 개별 보고"를 요구했으나 미구현이었다.

| 이음매 | p50 | p90 |
|---|---|---|
| left\|front | 0.0405 | 0.1349 |
| **front\|right** | **0.0526** | **0.1539** |
| right\|back | 0.0432 | 0.1527 |
| back\|left | 0.0364 | 0.1254 |

```
파노라마 내부  최소 0.016 / 중앙 0.044 / 최악 0.104     최악÷중앙 배수 p50 = 2.09
중앙 <= 0.055(G1 통과) 이면서 최악 > 0.10 인 경우 : 23.4%
```

**네 장 중 한 장꼴로 "보정 양호"라고 보고하면서 한 면 경계가 10% 이상 어긋나 있다.** 20m 에서
2m 차이다. 최악 이음매 위치도 `r|b` 60 / `f|r` 51 / `l|f` 33 / `b|l` 28 로 **오른쪽 면이 걸린
두 이음매에 몰린다** — 원인은 아직 모른다(우측 통행이라 주차·건물이 가까워 depth 가 나쁠 수도,
리그의 면 경계 위치 문제일 수도).

**조치**: `seam_residuals()` 로 4개를 그대로 돌려주고 `seam_per`/`seam_max`/`seam_worst` 를 저장.
계산은 어차피 4번 하므로 추가 비용 0. **G1 을 최악 기준으로 다시 볼 근거가 생겼다**(현재 통과는
중앙값 기준이라 과대평가일 가능성이 크다).

## X-19. 정합 신뢰도 지표 재평가 — 양방향은 기각, 전/후방 분리는 기록만

`fit_score` 의 한계 둘이 실측으로 확인됐다.
- **20m 이내만 본다**: 한 사례에서 근거리 99.8% / 원거리(20~40m) **60%**. 점의 98%가 20m 안에
  몰려 있어 원거리 어긋남이 평균에 묻힌다. `fit_near > 0.97` 인 파노라마의 **34.5%**가 `fit_far < 0.7`.
- **한쪽 포함률이다**: "파랑이 회색 안에 드는가"만 본다.

**후보 8개 평가** (397장. 기준 A=도로등급 상관 0 에 가까울 것, B=`clip≥2` 집단을 낮게 볼 것)

| 지표 | 등급상관 | clip=0 | clip≥2 | 차이 |
|---|---|---|---|---|
| `fit_near` (현행) | −0.465 | 0.948 | 0.697 | −0.251 |
| `fit_far` | −0.349 | 0.546 | 0.323 | −0.223 |
| `cover` (역방향) | −0.326 | 0.340 | 0.311 | **−0.029** |
| `iou` (양방향) | −0.481 | 0.276 | 0.251 | **−0.026** |
| `fit_bwd` | −0.429 | 0.967 | 0.550 | −0.418 |
| **`fb_min`** (전·후 중 나쁜 쪽) | −0.452 | 0.933 | 0.486 | **−0.446** |

**양방향(cover·iou) 기각**: 정합이 정상이어도 GIS 도로의 60~70%는 가려지거나 시야 밖이라
0.3 대에 머문다(정상 0.34 vs 실패 0.31). 판별력이 없다. — "GIS 도로의 절반이 확인 안 됐다"는
것은 이상값이 아니라 **정상**이었다.

**전/후방 분리는 유효**: 회전 오차가 있으면 전방과 후방이 반대쪽으로 흔들리는데 합치면 상쇄된다
(`clip≥2` 집단: fwd 0.890 / bwd 0.550). `fb_min` 의 검출력은 현행의 **1.8배**.

**그러나 판정에는 쓰지 않는다** — 등급 상관이 −0.452 로 현행과 같아 게이트로 쓰면 또 이면도로만
잘려나간다(X-05 의 실패 재현). `fit_far`/`fit_fwd`/`fit_bwd` 를 **기록만** 한다.

## X-20. `R_MIN_EGO` 는 면별이어야 한다

**발단**: 카메라 우측 1.4m 의 벽돌벽이 통째로 사라졌다. 세그멘테이션은 정확했다
(`right` 면 `detections=[(1,'building')]`, blocker 65,347/65,536 = 99.7%). `R_MIN_EGO=3.0` 이
65,330개 점을 전부 잘라 **그 방향이 `r=40m`(완전 트임)로 보고**됐다.

**규모**: 3m 이내로 버려지는 blocker 점 p50 0.4% / p90 19.3% / max 90.4%.
한 면의 절반 이상이 3m 이내인 파노라마 **7.2%**.

**부각 기준은 성립하지 않았다**: 자차가 고정 위치를 차지한다면 픽셀별로 "거의 항상 3m 이내"인
영역이 있어야 하는데, 200장에서 **70% 이상 일관된 픽셀이 0개**였다(최대 34.5%). 이유는
**자차가 `blocker` 에 거의 없기 때문** — 01 이 `vehicle_mask` 로 분리한다. front/back 근접빈도
1% vs left/right 10%.

**면별 임계 실측** (740장, 같은 edge 이웃과의 spearman)

| 설정 | 차폐율 | 차량 뒤 제외 | **이웃 spearman** |
|---|---|---|---|
| 전역 3.0 (구) | 0.188 | 0.059 | 0.591 |
| 전역 2.0 | 0.213 | **0.153** | 0.586 |
| fb3.0 / lr1.5 | 0.193 | 0.060 | **0.603** |
| **fb3.0 / lr1.0 (채택)** | 0.194 | 0.060 | **0.598** |
| fb3.0 / lr0.5 | 0.195 | 0.060 | 0.595 |
| fb2.0 / lr1.0 | 0.215 | 0.153 | 0.580 |

**앞뒤를 3.0 으로 유지하는 것이 핵심**이다. 전역 2.0 은 전방 차량까지 근접 차단물로 잡아
`road_unknown_frac` 이 0.060 → **0.153** 으로 뛴다. 좌우는 0.5 까지 내려도 전역 2.0 보다 낫다.

**남는 한계**: 수직거리 1m 이내의 **평행한** 벽은 정면 방향이 여전히 지워진다. 벽이 평행하면
정중앙이 가장 가깝고(1.4m) 비스듬할수록 멀어(θ=60°에서 1.62m), 임계가 **가장 가까운 정중앙만
정확히 도려내기** 때문이다. 발단이 된 사례는 lr=1.0 으로 대부분 살아난다.

## X-21. 정합의 회전이 근거리 점 개수에 압도당하고 있었다

**발단**: 그림에서 두 지도가 눈에 띄게 틀어져 보인다는 지적. 근거리는 맞는데 원거리가 어긋난다.

**원인**: `refine_pose` 가 도로 픽셀을 **점 개수**로 히스토그램에 넣었다. 원근 때문에
`0~10m 23,299점 / 10~20m 1,692점` 으로 **14:1** 이다. 회전은 먼 점이 결정하는데(지렛대가
길수록 각도 오차가 크게 드러난다) 가까운 도로면은 조금 돌려도 폴리곤 안에 그대로 있어 점수가
변하지 않는다. **회전이 사실상 자유롭게 떠 있었다.**

**가중 방식 비교** (292장. 평가는 `fit_far` = 20~40m 포함률, 어느 방식도 적합에 쓰지 않은
out-of-sample 구간이고 문제로 지적된 값 그 자체다)

| 가중 | `fit_far` 평균 | 짝지은 차이 | 개선 / 악화 | 한계 도달 |
|---|---|---|---|---|
| `count` (현행) | 0.519 | — | — | 2.4% |
| **`binary` (채택)** | **0.551** | **+0.0314** | **43.5% / 14.7%** | **1.4%** |
| `range` (거리 가중) | 0.535 | +0.0155 | 24.0% / 9.6% | 2.7% |
| `far` (10~20m 만) | 0.540 | +0.0204 | 40.1% / 24.0% | 3.8% |

**채택**: 셀 점유(0/1). 단위 **면적**당 동일 가중이 되어 원거리가 제 몫을 갖는다. `dyaw` 가
73.3% 의 파노라마에서 바뀐다(중앙 2°). `far`(먼 점만)도 개선되지만 악화가 24%로 불안정하다.

**실제 사례** (`point_10037`, 지적받은 그림)

| | 이전 | 이후 |
|---|---|---|
| `dyaw` | +12° | **+2°** |
| `fit_far` | 0.510 | **0.793** |
| `road_occluded_frac` | 0.364 | **0.000** |
| `road_unknown_frac` | 0.312 | 0.000 |

**12° 오차가 GIS 도로를 건물 쪽으로 밀어 "안 보이는 도로" 36.4% 를 만들어내고 있었다.**
X-08(tilt 클램프)과 같은 종류 — 기하 오차가 위험도로 둔갑한다.

**기록해 둘 것**: 사용자가 제안한 "앞뒤 두 방향으로 확장"의 본질이 이것이었는데, 처음에
**평가 지표** 얘기로 잘못 알아듣고 `fit_fwd`/`fit_bwd` 를 기록만 하는 데 그쳤다(X-19).
전/후방을 명시적으로 분리해 풀지 않아도, 가중을 면적 기준으로 바꾸면 앞뒤 먼 관측이 자동으로
제 무게를 갖는다.

## X-22. 전량 재실행 검증 — 무엇이 개선되고 무엇이 남았나

**표본**: `03_bev2`(구) ∩ `03_bev3`(신) = **36,337장 짝지음**. 두 실행의 차이는 D-12·D-14~D-18 ·
X-20 · X-21 · 부록 A 의 `PEN_XY` 정정 전부다. 개별 기여를 분리한 것이 아니라 **합산 효과**다.

### 1. 정합 — 큰 회전 오차가 대부분 해소됐다

| | 구 | 신 |
|---|---|---|
| `\|dyaw\|` 중앙 / 평균 | 2.0° / 4.30° | 2.0° / 4.23° |
| `\|dyaw\| ≥ 12°` | 9.9% | **8.4%** |
| `\|dyaw\|` 한계 24° 도달 | 1.6% | **1.3%** |
| `\|dx\|` 또는 `\|dy\| ≥ 8m` | 5.6% | **10.8%** ⚠ |

`dyaw` 가 71.4% 에서 바뀌었고(변경분 중앙 4°), **구버전이 회전 오차 12° 이상이던 3,591장 중
68.2% 가 12° 미만으로 내려왔다.** X-21 이 노린 지점이 그대로 나왔다.

이동이 한계(±8m)에 붙는 비율이 두 배가 된 것은 부록 A 의 `PEN_XY` 정규화 정정 때문이다 —
모서리 페널티가 2배였던 것을 바로잡자 이동이 그만큼 자유로워졌다. **수식은 옳아졌지만 결과는 관찰해야 한다**:
탐색창 ±8m 가 10.8% 에게 부족하다는 뜻이고, X-07 에서 창 확대를 기각했으므로 남은 선택지는
페널티 계수 재조정이다. §0.2-B 의 열린 항목.

### 2. 원거리 정합은 여전히 나쁘다 — 남은 최대 결함

| `fit_far` (20~40m 포함률, **적합에 미사용**) | 값 |
|---|---|
| 평균 / p50 / p10 | 0.563 / 0.580 / **0.187** |
| **< 0.5 비율** | **40.4%** |
| 참고: `fit_fwd` / `fit_bwd` (근거리) | 0.816 / 0.825 |

근거리 0.82 대 원거리 0.56 의 격차가 이 파이프라인에 남은 가장 큰 정합 결함이다. X-21 이
**회전**은 잡았지만 거리에 따라 벌어지는 성분(스케일·평면 기울기·GIS 폭 오차 중 무엇인지 미분리)은
남아 있다. `fit_far` 는 적합에 쓰지 않은 구간이라 이 값 자체가 정직한 out-of-sample 성적표다.

### 3. 정합 개선과 차폐율 감소의 인과가 확인됐다

| `dyaw` 변화량 | n | Δ차폐율 (신−구) | 신 `fit_far` |
|---|---|---|---|
| 변화 없음 | 10,410 | −0.001 | 0.674 |
| ≤ 4° | 16,398 | −0.011 | 0.575 |
| 4~12° | 6,946 | −0.051 | 0.450 |
| **> 12°** | **2,583** | **−0.098** | **0.346** |

**회전이 크게 틀려 있던 지점일수록 차폐율이 크게 떨어졌다.** 구버전이 GIS 도로를 엉뚱한 방향에
놓고 "저 도로가 안 보인다"고 세던 것이 바로잡혔다는 뜻이며, X-21 의 단일 사례(`point_10037`,
36.4% → 0.0%)가 전량에서 재현된 것이다. X-08(tilt 클램프)과 같은 계열 — **기하 오차가 위험도로
둔갑한다**는 이 프로젝트의 반복 패턴.

동시에 그 집단은 신버전에서도 `fit_far` 가 0.346 으로 가장 낮다. **다음 개선 대상이 어디인지를
같은 표가 알려준다.**

### 4. 차폐율

| | 평균 | p50 | p75 | p90 | 0인 비율 |
|---|---|---|---|---|---|
| 구 | 0.156 | 0.049 | 0.254 | 0.476 | 47.8% |
| 신 | **0.134** | 0.046 | 0.223 | **0.397** | 48.5% |

짝지은 차이 평균 **−0.0222**, 중앙 ±0.000. 감소 25.3% / 증가 19.0% / 동일 55.8%, Spearman
**0.829**. 전체가 눌린 것이 아니라 **재배열**됐다. 차량 뒤라 분모에서 뺀 비율(D-15)은 평균
**0.043** 으로, 전역 `R_MIN_EGO=2.0` 실험에서 나왔던 0.153 대비 정상 범위다 → **X-20 확인**.

### 5. DSI 와 등급 — tercile 이 이번에도 불가능하다

| | 평균 | p50 | p75 | p90 | 최빈값 비율 |
|---|---|---|---|---|---|
| 구 | 1.329 | 1.2068 | 1.442 | 1.698 | 47.8% |
| 신 (n=34,626, `valid` 만) | 1.291 | 1.1913 | 1.384 | 1.578 | **49.6%** |

Spearman(신, 구) = 0.825.

**새 분포의 tercile: `T1 = 1.1500`, `T2 = 1.3124`** (구 1.1500 / 1.3656). `T1` 이 최빈값
`1.15`(= 차폐율 0 · `V_heavy` 0.15) 와 **정확히 일치**한다 — 49.6% 가 한 값에 묶여 있으므로
하위 1/3 경계를 그을 수 없다. X-11 에서 47.8% 로 이미 겪은 문제가 **더 심해졌다**(정합이
좋아져 허위 차폐가 사라진 결과라 원인 자체는 개선이다).

→ D-11 의 "의미 기반 임계"가 선택이 아니라 **필수**임이 재확인됐다. `T2` 만 유효 임계로 쓰거나
"차폐 없음"을 별도 등급으로 명시하는 방향.

### 6. 보정 품질과 판정 가능 여부

| | 구 | 신 |
|---|---|---|
| `seam_rms` 중앙 / p90 | 0.0469 / 0.1010 | 0.0463 / 0.0992 |
| `seam_max` 중앙 / p90 | — | 0.1028 / 0.2563 |
| `seam_max ≥ 0.3` / `≥ 0.5` | — | 6.6% / 1.3% |
| `seam_rms < 0.15` 인데 `seam_max ≥ 0.3` (숨은 불량) | — | **5.4%** |
| `\|pitch\|` / `\|roll\|` 중앙 | — | 0.80° / 0.89° |
| **tilt 한계 8° 도달** | (4° 시절 클램프 다발) | **0.0%** |
| `sight_flag` None (판정 보류) | 7.2% | **1.1%** |
| `sight_flag` True | 4.1% | 2.9% |

- **D-12 확인**: `TILT_MAX_DEG` 8.0 에서 클램프가 사실상 소멸했다(0.0%). 4.0 이 실제 기울기를
  자르고 있었다는 X-08 의 진단이 맞았다.
- **D-17 확인**: 구간 판정으로 보류가 7분의 1이 됐다.
- **X-18 확인**: `seam_rms` 만 보면 통과인데 최악 이음매가 0.3 을 넘는 장이 5.4% 다. 4개 개별
  저장이 없었으면 안 보였을 값. 다만 아직 **판정에 쓰지 않는다** → §0.2-B.

`valid` **95.3%** / 판정 불가 4.7% (보정 2.3% · 도로 미상 0.3% · pose 2.2%, 사유 중복 포함).
`status`: `ok` 96.7% · `few_ground` 2.3% · `tilt_clamped` 0.5% · `bad_scale` 0.5%.
`faces300`: 4면 67.9% · 3면 17.5% · 2면 12.3% · 1면 1.8% · 0면 0.5% (뒤 둘이 D-14 로 기각되는 2.3%).

## X-23. 건너뛴 28장의 정체

**결론: 로직 실패가 아니라 입력 좌표가 도로망에서 이탈한 지점이며, 조치는 불필요하다.**

`process_panorama` 가 None 을 반환한 28장(36,367 중 **0.08%**)의 실제 탈락 지점은
`road_occlusion` 의 `keep.sum() == 0` 이었다 — 27장은 도달 가능 링크의 중점이 **전부** 40m 밖,
1장은 40m 안에 3.0m 만 들어와 `tot <= 5.0` 에 걸렸다.

| 최근접 중심선 노드까지 거리 | 중앙 | 범위 |
|---|---|---|
| 정상군 (무작위 3,000장) | **2.4m** | p99 12.9m |
| 건너뛴 28장 | **48.9m** | 34.8 ~ 123.1m |

`reachable()` 이 링크를 8~21개씩 돌려준 것은 최근접 **노드**에서 도로를 따라 40m 를 걸은
결과이고, 그 노드 자체가 이미 카메라에서 40m 밖이라 BEV 반경 필터에서 전멸했다.

**자기정합성**: 무작위 3,000장 중 노드까지 40m 초과가 0.067%, 실제 탈락률이 28/36,367 =
0.077%. 두 값이 일치하므로 다른 원인을 찾을 필요가 없다.

**분포**: `edge_id` 가 몰려 있다 — edge 4500 에 6장(`point_70498`~`70509` 연속), 4013·4111·
4152·6023 에 각 3장. 한 구간이 통째로 이탈했다는 뜻이라, 해당 도로가 중심선 데이터에 없거나
그 구간에 스트리트뷰가 없어 네이버가 다른 도로의 파노라마를 반환한 경우다. **둘 중 어느
쪽인지는 확인하지 않았다** (요청지점 `point_lat/lon` 대비 이탈 거리를 보면 갈린다).

값을 내도 근거가 없는 지점이므로 커버리지 99.92% 로 종료한다.

## X-24. 방위각 메타데이터 검증과 탐색창 축소

**발단**: 정합 실패(구 `clipped ≥ 2`, 797장) 진단 중 `dyaw` 가 무엇을 고치고 있는지 물었다.

### 어느 컬럼이 방위각인가

CSV 에 `camera_angle`(`[0, 184.86, 0]` 형태, 첫·셋째 원소는 전량 0)과 `heading` 이 따로 있고
둘의 차이는 중앙 −34.3°, 1° 이내 일치가 0.6% 였다. 어느 쪽이 맞는지 검증된 적이 없었다.

| 기준 (이미지 미사용) | `camera_angle[1]` | `heading` | 무작위 기대 |
|---|---|---|---|
| 도로축과의 차이 mod 180° (연속쌍 31,764) | **중앙 0.6°, <15° 92.2%** | 38.1°, 19.7% | 45°, 16.7% |

`heading` 은 무작위와 구분되지 않는다. `tilt=10`·`fov=90` 이 전량 상수인 것과 묶어 보면
`heading/tilt/fov` 는 수집 당시의 API 요청 파라미터다.

### 0/180 모호성은 존재하지 않는다

`pano_date` 가 **초 단위**(`2024-09-05 15:07:20`)라 촬영 순서 = 실제 주행 방향을 복원할 수 있다.
연속 촬영쌍 28,114개(Δt 중앙 1.0s, 속도 중앙 33km/h):

```
camera_angle[1] − 주행 방위각    중앙 −0.00°   |차이|>150° = 0.0%  (28,114쌍 중 0건)
```

계획서가 "무관하니 넘어간다"고 유보했던 항목이 **해소**됐다. 앞면은 앞을 본다.

### 좌표계도 맞다

EPSG:5179 는 중앙자오선 127.5°E 의 TM 이라 강릉(128.9°E)에서 격자북이 진북과 **+0.857°**
어긋난다(이론값 `(λ−127.5)·sin φ` 와 실측이 소수점 셋째 자리까지 일치). `camera_angle[1]` 의
잔차는 격자북 기준 −0.001°, 진북 기준 −0.859° — **메타데이터가 이미 격자북 기준**이므로
`_world_to_bev` 의 현행 취급이 옳다. 수렴각 보정은 필요 없다.

### 그래서 탐색창 ±24° 는 근거가 없었다

직진 구간 18,022쌍에서 방위각 잔차는 **로버스트 σ 0.43°**(중앙 |잔차| 0.3°, p95 2.9°).
±24° 는 **56σ** 다. 결정적으로 `|dyaw|` 구간별 메타 잔차가 평평하다 — 0~2° 구간 0.3°,
6~12° 구간 0.4°, **20~25° 구간 0.5°**. 20~25° 를 돌린 장의 방위각도 0.5° 정확하다.
`corr(dyaw, −잔차) = +0.046` — **회전 보정은 방위각 오차를 고치고 있지 않다.**

### 상수 −2° 는 실재한다

0.25° 격자로 직접 재면 중앙 **−2.00°**, 로버스트 σ **4.08°**. 촬영 세션(10분 이상 끊김 기준)별
중앙값은 −1.25 ~ −2.75° 로 전부 음수이고 세션 간 산포는 0.56° — 표본오차 수준이라 **단일 상수**로
설명된다. 파노라마당 잡음 4.08° 는 메타 오차의 10배다.

출처는 데이터가 아니라 우리 재구성 쪽이다: 네이버 궤적 ↔ GIS 중심선 축은 **+0.004°** 로 정렬돼
있는데 사진 도로면 ↔ GIS 는 −1.27° 다. 크롭은 정확하고(1536/6 = 256, 나머지 0) 기울기와도
무관하다(`corr(dyaw, roll) = −0.003`).

**기각된 가설**: 동점 처리 편향. `refine_pose` 가 `-24` 부터 오름차순으로 돌며 `if s > best[0]`
로 갱신하니 동점이면 더 음수가 이긴다고 의심했으나, 순서를 뒤집어도 `dyaw` 가 **100% 동일**
하고 점수 차이가 정확히 0 이었다. 동점이 아니라 최적점이 유일하다.

### 이동 ±8m 는 근거리에 과적합이었다

창을 좁힐수록 **적합에 쓴 구간은 나빠지고 안 쓴 구간은 좋아진다** — 과적합의 교과서적 신호다.

| 회전창 | 이동창 | `fit_far`(20~40m) | 근거리(≤20m) | 이웃 spearman |
|---|---|---|---|---|
| ±24 | ±8 | 0.585 | 0.837 | 0.574 |
| **±6** | **±3** | **0.595** | 0.819 | 0.629 |
| ±6 | ±1 | 0.586 | 0.776 | 0.647 |
| 0 | 0 | 0.553 | 0.705 | 0.730 |

이동 최적은 ±3m(±2~4 평평, ±1 에서 꺾임), 회전은 ±2 에서 이미 포화. 상수 −2° 를 적용하면
같은 창에서 고정 0°(0.590) 대비 고정 −2°(0.595) 로 +0.005 를 얻는다.

**주의 — 이웃 spearman 은 정확도 지표가 아니다.** 아무 보정도 안 하면(0,0) 0.730 으로 가장 높다.
**`fit_far`(정확도)와 이웃 일치도(잡음)가 동시에 좋아지는 지점**만 진짜 개선이다.

**전량 실행 결과**: `fit_far` 0.563 → 0.568, 근거리 0.820 → 0.803, 이동 중앙 3.2 → 2.8m,
**실행 시간 90분 → 60분**(`DYAW` 25개 → 5개라 FFT 가 1/5). 개선폭은 스윕 예측(+0.010~0.012)의
절반이었다 — 스윕 표본이 edge 당 3장 이상인 곳만 골라 전량보다 쉬운 집단이었기 때문으로 보인다.

## X-25. 게이트 후보 within-edge 전수 검정

**발단**: 옛 pose 게이트가 잘 맞히는지 물었다. 실측은 정반대였다 — 오탈락 200장(정합 양방향
≥0.8 인데 버려짐) 대 **오통과 2,392장**(`clip ≤ 1` 인데 양방향 <0.5)으로 12배 틀린 방향이었다.

**두 개의 실패 모드가 갈린다** (판별자: 이동 방향이 국소 도로 축과 이루는 각)

| 집단 | n | 도로 따라감(<30°) | 가로지름(>60°) | 정합 전 겹침 `ov0` |
|---|---|---|---|---|
| 정상 clip=0 | 23,461 | 35.2% | 36.6% | 0.687 |
| **미끄러짐형** | 559 (70%) | **56.0%** | 19.3% | 0.413 |
| **위치오차형** | 238 (30%) | 30.9% | **44.3%** | **0.034** |

무작위 기대는 33.3%/33.3% 이고 정상군이 정확히 그렇다. 두 실패군만 방향성을 갖고 **서로 반대**다.

- **미끄러짐형**: 도로 축 방향 위치는 도로면 정합으로 **식별되지 않는다**(직선 도로는 10m 앞뒤가
  똑같이 생겼다). `PEN_XY=0.04` 라 코너까지 11.3m 가는 비용이 겹침 4%p 뿐이라 붙잡히지 않았다.
  창을 넓혀도(±24m) 20.8m 로 더 미끄러지고 `fit_far` 는 +0.006 뿐. **잡음이지 편향이 아니다**:
  축 성분을 제거해도 평균이 0.336 → 0.333 로 그대로고 개별 값만 중앙 0.059 움직인다.
- **위치오차형**: `ov0` 중앙 0.034(63% 가 0.1 미만). 24m 를 움직여도 `fit_far` 0.335 → 0.361 로
  이동으로 고쳐질 문제가 아니다. X-23 의 28장과 같은 계열의 약한 버전.

**여기서 처방 원칙이 나온다**: 오차가 **잡음**이면 추정기를 고친다(게이트는 데이터만 줄이고
편향을 못 없앤다). **편향**, 특히 위험을 낮추는 방향이면 게이트로 막는다.

**후보 검정 (전량 34,663장 / edge 3,496개)** — 지표는 `|occ − 같은 edge 나머지 평균|` 의 차.
대칭 LOO 라 대조군이 0 이 나온다.

| 후보 | 적중률 | 이탈도 차 | t | 초과차폐 |
|---|---|---|---|---|
| 무작위 50% *(대조)* | 50.0% | −0.002 | −1.4 | +0.003 |
| **옛 `clipped ≥ 2`** | 29.3% | **+0.005** | 3.0 | +0.032 |
| `fit_score < 0.5` | 17.2% | −0.005 | — | +0.020 |
| `min(fwd,bwd) < 0.3` | 7.5% | −0.003 | — | +0.073 |
| 이웃 pose 이탈 >4m | 12.0% | −0.003 | — | +0.054 |
| `ov0 < 0.10` | 2.8% | +0.052 | — | +0.132 |
| **폴리곤 밖 >2m** | 5.3% | **+0.028** | 4.3 | +0.102 |
| **`faces300 < 2` (D-14)** | 2.4% | **+0.160** | 14.2 | +0.297 |

`fit_score` 는 X-05 의 결론이 재확인됐다. D-14 는 압도적으로 강해 유지가 맞다.
채택은 `폴리곤 밖 >2m` → D-19.

**첫 시도의 지표 결함**: 처음엔 기준선을 비플래그군 평균으로 잡아 대조군이 **+0.075** 가 나왔다
(비플래그군의 자기 평균 대비 편차가 구조적으로 작다). 대칭 LOO 로 고치니 +0.001 이 되고
대부분의 후보가 판별력 0 으로 뒤집혔다. **대조군이 0 인지 먼저 확인할 것.**

**남은 관찰**: 새 창에서는 모든 후보의 초과차폐가 **양수**다 — 잔여 정합 오차가 위험을 낮게
말하지 않는다. 창을 좁힌 것이 게이트가 하려던 일의 상당 부분을 이미 해냈다.

## X-26. 회전을 기하로 직접 계산하려는 세 시도

**발단**: "겹침 최대화 말고 두 지도의 축이 평행하도록 회전을 정하면 어떤가."

X-24 가 "회전 = 상수 −2° + σ4° 잡음"이라 결론지었으므로, 회전을 **탐색이 아니라 계산**으로
정하면 잡음을 없앨 수 있다는 발상이다. 세 형태로 시도했다.

| 방식 | 근거 기하 | σ | `\|·\|>10°` | 중앙 |
|---|---|---|---|---|
| ① 도로면 PCA 축 | 사진 도로면의 장축 | 9.66° | 37.0% | −2.28° |
| ② 방사 프로파일 | 도로가 뻗은 갈래들의 각도 분포 | 5.93° | 18.3% | −1.50° |
| ③ 회랑 벽면 | 양옆 벽면의 평행선 | 7.86°→**4.22°** | 30.9%→13.9% | −1.45° |

**① 실패 원인은 측정됐다** — 차 안에서 보이는 도로면은 조건수 `s2/s1` 중앙 **0.480**,
즉 장축:단축 **2.1 : 1** 로 거의 등방이다(0.5 초과가 47.8%). PCA 로 축을 뽑을 형상이 아니다.

**② 는 갈래 각도 분포의 원형 상호상관.** 장축 하나가 아니라 형상 전체를 쓰므로 ① 보다 낫지만
여전히 부족했다.

**③ 회랑 벽면은 기하가 가장 유리하다** — 벽면은 폭 1~2m 에 길이 40m 라 20:1 이상이다.
`r(θ)` 가 곧 가시 장애물 윤곽이므로 **이미지 없이** 계산된다. 방향 α 후보마다 점을 수직축에
투영해 집중도가 최대인 α 를 고른다.

**③ 의 자체 검증 — 검출 폭이 실제 도로폭과 일치한다**

| 등급 | GIS `ROAD_BT` 중앙 | 회랑 검출 |
|---|---|---|
| 4 (길) | 10.0m | 10.5m |
| 3 (로) | 20.0m | 20.0m |
| 2 (대로) | 25.0m | 21.0m |

**오차를 분리하니 GIS 쪽이 절반이었다.** 주행 방위각(정확도 0.43°)을 외부 기준으로 쓰면
`dyaw = ψ − α − bearing` 의 두 성분을 뗄 수 있다:

| 성분 | σ |
|---|---|
| 사진 회랑 (α+bearing) | 3.98° (고신뢰 `frac≥0.75` 에서 **2.48°**) |
| GIS 중심선 원판 PCA (ψ) | 2.73° |

GIS 축을 원판 PCA 대신 **도로를 따라 걸으며**(옆길로 꺾이는 가지는 배제) 뽑자 σ 2.73° → **1.43°**,
이상치 16.7% → 5.0%. 합성 `dyaw` 도 7.86° → **4.22°**(고신뢰 2.65°)로 개선됐다.

**그럼에도 `fit_far` 로는 전부 현행에 졌다.**

| 회전 결정 | `fit_far` | 근거리 | 이웃 spearman | 짝지은 차이 |
|---|---|---|---|---|
| **현행 탐색** | **0.594** | 0.808 | 0.652 | — |
| 고정 −2° | 0.591 | 0.803 | 0.646 | −0.0023 |
| ③ 회랑 (신뢰 ≥0.75) | 0.590 | 0.804 | 0.638 | −0.0042 |
| ③ 회랑 축소 0.5 | 0.588 | 0.803 | 0.646 | −0.0057 |
| ③ 회랑 전체 | 0.572 | 0.800 | 0.630 | −0.0221 |

(② 방사 프로파일은 별도 표본에서 0.600 vs 현행 0.608.)

**회랑이 가장 자신 있는 구간에서도 진다.** 손해가 신뢰도 순으로 단조 증가하므로 신뢰도 지표
자체는 품질을 옳게 정렬하지만, 최고 구간에서조차 이기지 못한다.

| 신뢰도 `frac` | n | 현행 | 회랑 | 차이 | 개선 / 악화 |
|---|---|---|---|---|---|
| ≥0.75 | 432 (25.4%) | 0.628 | 0.618 | −0.0098 | 19.0% / 29.9% |
| 0.60~0.75 | 463 (27.2%) | 0.605 | 0.587 | −0.0180 | 17.7% / 32.8% |
| <0.60 | 808 (47.4%) | 0.569 | 0.538 | −0.0310 | 13.9% / 41.4% |

**결론: 세 방식 모두 기각. 회전은 현행 겹침 탐색을 유지한다.**

**여기서 두 가지가 확정된다.**
1. **σ 로 회전 추정기를 비교하면 안 된다.** 현행 탐색의 σ 2.97° 는 창이 −2±4 로 잘려 구조적으로
   작은 값이고, 극단적으로 상수 고정은 σ=0 이지만 최선이 아니다. 기준은 `fit_far` 다.
2. **고정 −2° 도 현행보다 나쁘다**(−0.0043). 회전이 "상수 + 순수 잡음"이었다면 상수가 이겨야
   한다. 졌다는 것은 겹침 탐색이 상수 너머의 실제 정보를 **미약하게나마** 뽑고 있다는 뜻이다.
   X-24 의 "상수 + 잡음" 정리는 절반만 맞았다.

**부산물**: 원리가 전혀 다른 세 추정기가 모두 −1.5 ~ −2.3° 를 가리켰다. `YAW_OFFSET = −2.0` 이
추정기 인공물이 아니라 실재하는 계통 오차임이 교차 검증됐다.

**교훈**: 두 추정값의 **차**를 볼 때는 어느 쪽 오차인지 먼저 분리할 것. ③ 을 처음에 기각할
뻔했는데, 사진 쪽은 처음부터 가장 좋았고(σ 3.98°) 내가 잡음 많은 GIS 축과 빼면서 망치고 있었다.

## X-27. 평가 기준을 `fit_far` 에서 잔여 회전(도)으로 바꾸다

**발단**: "각도가 조금씩 틀어지는 게 아쉽다. 평가 기준을 바꿔볼까?"

### `fit_far` 는 회전을 볼 지렛대가 없다

`fit_far` 는 **포함률**(사진 도로 점이 폴리곤 안에 드는 비율)이라, 폭 10m 폴리곤 안의 폭 6m 도로
띠가 30m 지점에서 3° 돌아 1.6m 밀려도 여전히 안에 있어 점수가 안 변한다. 실측 감도:

```
회전 1° 당 fit_far 변화  0.0026     파노라마 간 표준편차  0.2568   ->  신호/잡음 0.010
```

반응 자체는 단조·대칭이라(±1° −0.0026, ±2° −0.0108, ±6° −0.0749) **짝지은 비교로는 감지된다** —
X-26 의 순위(−0.0098 ≈ 3.8° 상당)는 유효하다. 다만 1° 급을 다루기엔 지렛대가 250배 부족하고
회전·이동을 한 숫자로 뭉갠다.

### 대안: 가로 이탈의 기울기

GIS 도로 축(**도로 따라 걷기**, X-26)을 기준선으로 잡고 사진 도로 점의 부호 있는 수직 이탈을
도로 방향 좌표에 회귀한다. `along` 5m 구간마다 `perp` 중앙값을 취해 강건화한다.

```
perp = a + b·along      b = 잔여 회전(라디안),  a = 가로 이동 오차
```

회전과 이동이 **분리**되고 단위가 도(°)라 해석된다. 감도는 1° 당 0.665° (과소반응하지만 선형).
폭 교란에도 덜 걸린다 — 포함률과 달리 기울기는 폴리곤 폭과 무관하다.

**순환 위험**: 회랑 방식(X-26 ③)도 같은 GIS 축을 쓰므로 유리하게 기울 수 있다. 그래서
**주행 방위각**(`pano_date` 기반, 정확도 0.43°, 어느 후보도 쓰지 않음)으로도 함께 쟀다.
두 기준의 결론은 일치했다.

### 이 자로 재니 탐색이 상수보다 나쁘다 → D-21

| 회전 결정 | \|잔여회전\| 중앙 (GIS) | (주행, 독립) | >2° | `fit_far` |
|---|---|---|---|---|
| 탐색 (구) | 2.25° | 1.96° | 49.3% | 0.670 |
| **고정 −2°** | **2.13°** | **1.77°** | **44.0%** | 0.668 |
| 회랑 0.75 | 2.21° | 1.91° | 47.6% | 0.665 |
| 회랑 전체 | 2.74° | 2.49° | 58.7% | 0.650 |

**회랑은 새 자로도 진다** — 최고 신뢰 구간(`frac≥0.75`)에서도 현행 1.78° 대 회랑 2.53°.
X-26 의 기각은 자를 바꿔도 유지된다. 상세 근거는 D-21.

### 회전 오차가 최종 지표에 매기는 값

`r(θ)` 는 카메라에 고정돼 옳으므로 bearing 만 흔들면 순수한 회전 비용이 나온다 (4,639장).

| 회전 오차 | \|Δ차폐율\| 중앙 | 평균 | >0.05 | **등급 변경** |
|---|---|---|---|---|
| 1° | 0.0000 | 0.011 | 10.5% | 5.8% |
| **2°** | 0.0000 | 0.019 | 17.1% | **9.7%** |
| 3° | 0.0000 | 0.028 | 23.1% | 13.5% |
| 5° | 0.0009 | 0.047 | 34.4% | 20.7% |

**중앙값이 0인 것이 핵심** — 차폐율 0 인 트인 곳이 절반이라 회전과 무관하다. 그러나 꼬리가
무겁다. 비교하면 이번 라운드 전체 변경이 만든 차폐율 이동이 평균 0.008 이므로, **잔여 회전
2~3° 의 비용(0.019~0.028)이 그보다 크다.**

### 잔여 회전은 측정 잡음이 아니다

| 성분 | 값 |
|---|---|
| 관측 \|잔여회전\| 중앙 / 로버스트 σ | 2.20° / 3.16° |
| **회귀 표준오차 (지표 자체의 정밀도)** | **1.31°** (p90 2.81°) |
| → 참 회전 오차 σ | **≈ 3.1°** |

**곡률이 원인이 아니다.** 2차 적합으로 곡률을 분리한 1차항은 2.28° 로 오히려 미세하게 크고,
가장 곧은 도로(`s2/s1 < 0.05`, 표본의 82%)에서도 1.99° 가 남는다.

**보정 기울기도 아니다.** `corr(|잔여회전|, |pitch|) = +0.063`, `|roll| = −0.005`,
`status=ok` 만 봐도 2.15°.

**edge 내가 더 크다**: edge 간 σ 4.16° / edge 내 σ 5.00° (2차 기준). 장소의 성질이 아니라
**파노라마별 재구성 오차**다.

**남은 후보**: 면별 affine 보정. `ground_bev` 는 깊이가 아니라 적합된 평면만 쓰는데, 그 평면은
4면의 지면 픽셀로 함께 적합된다. 면마다 지면 픽셀 수가 다르면(실측 4419/5578/6540/6164) 평면이
많이 보이는 면 쪽으로 끌려가고 그 좌우 비대칭이 도로면 투영을 회전시킬 수 있다.
**`n_ground_px` 의 좌우 비대칭과 잔여 회전의 상관으로 검정 가능** → §0.2-B.

### 실패한 설계 하나 (반복 금지)

**반쪽 분할로 잡음을 재려 했다** — 전방 절반과 후방 절반으로 각각 재고 차이의 분산에서 잡음을
추정하는 방식. 잡음 σ 9.42° 가 나와 관측 산포 4.95° 보다 커졌고 참 분산이 음수가 됐다.
원인: 절반만 쓰면 `along` 범위가 반토막 나고 기울기 표준오차는 `1/√Sxx` 로 커지므로 **반쪽
적합의 잡음**을 재게 된다. 전체 적합의 잡음이 아니다. **회귀 표준오차를 직접 쓰는 편이 옳다.**

---

# §4. 방법론

## 4.1 within-edge 짝지은 검정 — 이 프로젝트의 표준 도구

**방법**: 같은 `edge_id` 안에 플래그 True/False 가 섞인 경우만 골라, True 그룹의 차폐율에서
같은 edge 의 False 그룹 평균을 뺀다. edge 하나가 도로 등급·폭·지역·건물 밀도를 고정한다.

**방법 자체의 검증(대조군)**: 무작위 50% → **+0.002**, `ok` vs 같은 edge 다른 `ok` → **+0.000**.

**왜 필요한가**: 이 프로젝트에서 **전역 상관은 믿을 수 없다.** 모든 품질 신호가 "좁고 복잡한
골목"이라는 동일한 물리적 성질과 얽혀 있고, 그 성질이 동시에 실제 차폐를 만든다. 좁은 골목에서는
지면이 덜 보이고(`few_ground`), 기울기가 크고(`tilt_clamped`), 폴리곤이 가늘고(낮은 `fit_score`),
**그리고 실제로 더 가려진다.**

**전수 검정 결과**

| 신호 | 해당 비율 | within-edge 초과 차폐 | 판정 |
|---|---|---|---|
| `calib.status != ok` | 8.3% | **+0.220** | 진짜 편향 → X-08, X-09 |
| pose 탐색한계(±8m) 도달 | 5.6% | +0.070 | 약함 → X-07 |
| seam 잔차 상위 10% | 10% | +0.046 | 약함 |
| `fit_score < 0.7` | 23% | +0.008 | **무의미** → X-05 |
| `fit_score < 0.5` | 7% | −0.017 | **무의미** |
| `starved_bins > 200` | 8% | −0.031 | 트인 곳 → 4.3 G4 |
| *(대조) 무작위 50%* | 50% | *+0.002* | — |

## 4.2 hold-out 정합 평가

정답이 없는 상태에서 pose 설정을 비교하는 방법. **근거리(≤10m) 점으로만 정합하고 중거리
(10~20m) 점의 폴리곤 포함률로 평가한다.** 옆 도로로 미끄러진 해는 근거리는 맞춰도 중거리를
못 맞춘다. 폴리곤이 넓으면 포함률이 공짜로 높지만, **같은 파노라마 안에서 설정끼리 짝지어
비교하므로 상쇄된다** (절대 점수로는 쓸 수 없다 — 등급별 중앙 0.956/0.860/0.583).

## 4.3 게이트와 실제 판정

| 게이트 | 기준 | 실측 (전량 36,337장, X-22) | 판정 |
|---|---|---|---|
| **G1** 이음매 잔차 | 중앙 ≤ 0.055 | **0.0463** | 통과 |
| **G1** `status=="ok"` | ≥ 85% | **96.7%** | 통과 |
| **G2** pitch/roll 활성화 | `std(Δtilt) < 1.0°` | 0.04° (면내 무작위 분할) | 통과 → 활성화. 전량에서 tilt 클램프 **0.0%** |
| **G3** 해석적 vs 래스터 A_shadow | 중앙 5% 이내 | — | **무의미해짐** (도메인 교체) |
| **G4** `starved_bins` | 중앙 ≤ 30/720 | 46 | **기준 폐기** ↓ |
| **G5** Spearman(신, 구) | ≥ 0.75 | 차폐율 0.829 / DSI 0.825 | 통과했으나 **판정 근거로는 무의미** — 도메인이 같아 상관이 높은 게 당연하다 |
| **G6** 파노라마 간 scale 스캔 | `s = 1.00 ± 0.10` | — | **유효·미실행** |
| **G7** JPEG 형상 | 정확히 649×2187 | — | **폐기** (D-18 로 가로 1748×727 전환) |

**게이트가 못 잡는 것**: G1 은 이음매 **중앙값**이라 최악 이음매를 통과시킨다 — `seam_rms < 0.15`
인데 `seam_max ≥ 0.3` 인 장이 5.4% (X-18·X-22 §6). 그리고 어떤 게이트도 **원거리 정합**을 보지
않는다 — `fit_far < 0.5` 가 40.4% 인데 이를 막는 기준이 없다. 둘 다 §0.2-B 의 열린 항목이다.

**G4 기준 폐기 근거**: `starved_bins` 가 클수록 점 개수가 적고(26,923 → 5,080), 차폐율이 0 이며,
`calib ok` 비율이 오히려 높다(0.879 → 0.951). within-edge 초과도 −0.031. **자료 부족이 아니라
볼 것이 없는 트인 곳**이며, 이 지표는 장면 밀도의 역수지 품질 지표가 아니다.

**G7 폐기 경위**: 세로 형상은 `bbox_inches="tight"` 때문에 **제목 길이가 그림 폭을 결정**했다.
제목에 진단값을 넣자 폭이 649 → 651/660/676/694 로 흔들렸고, 문자 수는 폭의 지표가 되지 못했다
(비례폭 글꼴이라 `W` 하나가 `i` 넷과 맞먹는다. 고정폭으로 바꿔 62자로 재면 결정론적이 되지만
그만큼 넣을 수 있는 정보가 줄었다). 웹 크롭을 고치는 편이 낫다고 판단해 가로로 전환했다(D-18).
가로에서는 그림이 제목보다 넓어 제약 자체가 사라진다.

**G6 (미실행, 유일한 절대 스케일 검증)**: 같은 edge 의 인접 파노(간격 10.7m)를 ENU 정렬 후
occupancy IoU. `Δyaw ∈ [−30°,30°]` + **scale `s ∈ [0.7,1.4]` 스캔**. GPS 기선이 외부 자로
작동한다. h=2.0 집단만 s=1.25 로 나오면 GIS 높이 확증 = 통과.

## 4.4 그 밖의 검증 수단

| # | 방법 | 증명 가능 / 불가 |
|---|---|---|
| a | **이음매 잔차** — 4개 중 2개를 적합에서 제외하고 그 2개로 평가 | 면 간 metric 정합성을 out-of-sample 로 증명. 4면이 일관되게 틀린 경우는 못 잡음 |
| b | **파노라마 간 IoU + scale 스캔** (= G6) | **절대 스케일 검증 — 유일한 방법**. 이웃 없는 edge 는 불가 |
| c | **적대적 시각 A/B** — 3면 이상 지면<300px / mountain·hill / tree>20% / ΔDSI 최대 | 특정 실패모드 해소 여부만 |
| d | **DSI 분포 이동 + Spearman** | 교란 크기의 상한만 |
| e1 | **held-out 지면 잔차** — road 로 적합, sidewalk 로 평가 | 깊이 모델 자체의 out-of-sample AbsRel. 비용 0 |
| e2 | **면내 무작위 분할** → `n` 안정성 | pitch/roll 활성화 게이트(G2). **면별 분할은 무효** — 2면으로는 `n` 을 고정 못 해 std 49.63° 가 나왔다 |

---

# §5. 대기 작업

## 5.1 웹 반영 — 버전 추가 절차

웹은 03 실행 한 벌을 **버전**으로 묶어 통째로 갈아끼운다. 이미지만 새 버전인데 색은 옛 버전인
상태를 만들지 않기 위해서다. 버전 하나 = 데이터 파일 다섯 벌 + `web/app/versions.ts` 항목 하나.

| 파일 | 만드는 법 | 어디에 |
|---|---|---|
| BEV 이미지 36,339장 | 03 노트북 | **Drive** |
| `point_detail_<v>/` 조각 100개 | `rebuild_dsi_data.py` 4단계 | **Drive** (이미지와 같은 폴더) |
| `bev_map_<v>.json` | `build_image_map.py <folder_id> bev_map_<v>.json _bev360` | 저장소 |
| `detail_map_<v>.json` | `build_detail_map.py <folder_id> <v>` | 저장소 (약 4KB) |
| `dsi_map_<v>` · `road_dsi_map_<v>` · `bus_route_dsi_<v>` | `rebuild_dsi_data.py <v> [bev_dir]` | 저장소 (약 5MB) |

1. `python web/scripts/rebuild_dsi_data.py <v> <output/폴더>` — 지표 4종 + 상세 조각 생성
2. `Google Drive upload.ps1` 의 `$LocalFolder`(이미지)와 `$DetailFolder`(상세 조각)를 이
   버전 것으로 바꾸고 실행 — **03 종료 후 한 번 더** 돌릴 것 (`--min-age 1m` 때문에 마지막
   파일이 빠진다). 이미지와 조각이 같은 Drive 폴더로 올라간다
3. Drive 폴더를 "링크가 있는 모든 사용자"로 공유 → `build_image_map.py` + `build_detail_map.py`
4. 임계 두 개를 그 버전 분포에서 재적합 — 지점은 D-11 의미 기반(차폐 0 을 Safe, 나머지의
   중앙값이 두 번째 경계), 도로는 지점 평균이라 최소값 쏠림이 사라져 tercile 그대로
5. 렌더 형상이 바뀌었으면 패널 좌표 실측 (흰 배경이 아닌 픽셀의 행·열 분포에서 경계를 찾는다)
6. `versions.ts` **맨 앞**에 항목 추가 — 첫 항목이 기본값이자 "(최신)" 라벨

**현재 등록된 버전**

| id | Drive 폴더 | 렌더 | 지점 임계 | 도로 임계 | 지점 상세 |
|---|---|---|---|---|---|
| `260819` | `latest_260819` | 가로 1748×727 | 1.1501 / 1.3711 | 1.243 / 1.4066 | 있음 (33,489) |
| `260818_2` | `260818_2` | 가로 1748×727 | 1.1501 / 1.382 | 1.2563 / 1.4244 | 있음 (34,628) |
| `260818` | `260818` | 세로 649×2187 | 1.1501 / 1.4375 | 1.2735 / 1.4754 | 일부 — `seam_per`·`fit_far`·`clipped` 없음 |
| `260811` | `260811` | 세로 649×2187 | 1.92 / 3.14 | 2.36 / 3.57 | 없음 (로컬 산출물이 남아 있지 않다) |

**판정 게이트는 실행마다 다르다.** 260819 부터 `pose_converged` 가 빠지고 `on_mapped_road`
(카메라가 GIS 도로망 위에 있는가, `cam_off_road_m` 동반)가 들어왔다. 판정 불가가 4.7% →
**7.8%** 로 늘었고 그중 70%(1,882 / 2,850)가 이 새 게이트다 — 도로망에 없는 길에서 찍힌
파노라마다. 웹 패널은 어느 게이트가 막았는지에 따라 그 지표 줄을 빨갛게 칠하므로, 게이트를
바꾸면 `build_point_detail.py` 의 필드 목록과 `PointDetailPanel` 의 표시 줄도 같이 고쳐야 한다.

**DSI 는 버전 간 비교할 수 없다.** 260811 은 `D_stop/L_vis` 를 곱한 옛 정의라 최대 638 까지
벌어지고 260818 이후는 도로 차폐율 기반이라 1.15~2.30 이다. 그래서 임계도 버전마다 따로 둔다 —
**같은 색이라도 버전이 다르면 같은 위험도가 아니다.**

**폴더 이름 변경은 웹에 영향이 없다** — `bev_map_<v>.json` 은 경로가 아니라 **파일 ID** 를 담고
프론트가 `https://drive.google.com/thumbnail?id=...` 로 부른다. 실제로 `latest_260818` 은
`latest_260818_2` → `260818_2` 로 두 번 바뀌었지만 매핑은 그대로 동작했다.

`build_road_dsi_map.py` 의 `grade_of` 가 쓰는 폐기된 1.0/1.8 임계는 `grade` 필드에만 영향을
주고 프론트는 그 필드를 쓰지 않으므로(색은 `versions.ts` 임계로 다시 계산) 배포 정확성에는
영향이 없다. 우선순위 낮음.

**지점 상세는 Drive 에 두고 조각으로 낸다.** `point_detail_<v>/<point_id//1000>.json` 100개
(버전당 12MB). 저장소에 커밋하면 버전마다 12MB 가 히스토리에 영구히 쌓이므로
`.gitignore` 로 막고, 조각 번호 → fileId 인 `detail_map_<v>.json`(약 4KB)만 커밋한다.

한 파일로 두지 않는 이유는 패널이 지점 하나를 보여주려고 12MB 를 다 받게 되기 때문이다.
조각 하나는 약 150KB(gzip 29KB)다.

브라우저는 Drive 를 직접 부르지 않고 `/api/point-detail/<v>/<bucket>` 을 거친다. 이유 둘:
API 키가 브라우저로 나가지 않고, Drive 응답의 `Cache-Control: private, max-age=0` 을 여기서
`s-maxage=31536000, immutable` 로 바꿔 CDN 이 붙잡게 하기 때문이다(조각은 한번 올리면 바뀌지
않는다). **Vercel 환경변수에 `GOOGLE_DRIVE_API_KEY` 가 있어야 한다** — 없으면 이 경로가 500 을
낸다. 상세를 올리지 않은 버전은 404 가 나고 패널이 상세 절만 숨긴다.

옛 조각 200개(260818 · 260818_2)는 커밋 `129cf41` 에 이미 들어가 있어 히스토리에서는 지워지지
않는다. 인덱스에서만 빼서 앞으로의 배포에는 실리지 않게 했다.

## 5.2 문서

**웹이 두 파일을 라이브로 읽는다** — `web/app/page.tsx:126`(`readme.md`) 과 `:132`(`method.md`)
가 `raw.githubusercontent.com/<repo>/<branch>/<파일>` 을 그대로 가져온다. **main 에 push 하는
순간 공개 문서가 바뀐다.**

끝난 것:

- 옛 `method.md`(IPM 지면접점 · R=60 · 원판 `A_total` 을 서술하고 삭제된 `03_bev_shadow.ipynb`
  를 참조하던 문서)를 이 설계·실험 기록으로 **전면 교체**. Step 1~4 서술은 `readme.md` 로 이관.
- `readme.md` 3단계 재작성 — ①~⑥ 을 용어 대신 동작으로 서술, 캡션의 "점유 격자/계측 도메인"을
  풀어 씀. 옛 `method.md:130` 의 "정규화 기준이 뒤섞여서"(부정확 — 실제 이유는 기하 왜곡, X-02)
  는 교체 과정에서 함께 사라졌다.
- `readme.md` **전면 재작성** — 제안서 6단계 구성에 맞춰 범위·입력 데이터·산출물·저장소 구조·
  제안서 대비 변경표를 넣고, 단계별 예시 이미지와 라이선스 절을 붙였다.
- 웹 문서 모달이 표와 HTML(가운데 정렬 블록·이미지)을 렌더링하도록 `rehype-raw` 적용. 문서 안의
  상대 경로는 이미지=raw, 링크=GitHub 문서 페이지로 치환한다(이 사이트 도메인에서는 404 라서).
- `LICENSE`(MIT) · `NOTICE` 추가. 원본 파노라마 예시 이미지는 네이버 저작물이라 저장소에서
  내렸다 — 다만 커밋 `0aea1d7` 로 이미 push 되어 **과거 커밋에는 남아 있다**.

남은 것:

- `readme.md` 산출 수치 갱신 — 현재 "36,367지점" 만 있고 **실제 처리 36,339장(99.92%)**,
  판정 불가 4.7% 가 반영돼 있지 않다
- `readme.md` 의 등급 서술 — D-11 확정 후에 맞춰 쓸 것. 지금 "전체 분포 기준 3구간"이라고
  적혀 있는데 X-22 §5 대로면 사실이 아니게 된다

---

# 부록 A. 코드 결함 수정 기록

| 대상 | 결함 | 조치 |
|---|---|---|
| `03` RESUME | 입력 부족·도로망 미확보 파노라마는 파일을 남기지 않아 "완료분은 앞쪽 연속 구간"이라는 **단조성이 깨진다**. 이진탐색이 구멍에서 멈춰 뒷부분을 통째로 재처리 | 집합 차집합으로 교체 (글롭 2회, 0.001s) |
| `web/scripts/build_dsi_map.py` | `BEV_DIR` 가 삭제된 `output/03_bev_gpu` 를 가리킴. `Path.glob` 은 없는 디렉터리에서 예외 없이 빈 결과를 주므로 **0건짜리 `dsi_map.json` 을 쓰고 exit 0** → road/bus 까지 연쇄로 비워 지도가 통째로 "데이터 없음" | 경로 교체 + 디렉터리 부재/0건 시 덮어쓰기 거부 |
| `Google Drive upload.ps1` | ① 토큰 만료(`invalid_grant`)가 실제 원인인데 진단 없음 ② 원격 경로에 `\` 사용 (rclone 은 `/` 만 구분자로 보고 `\` 는 폴더 이름의 일부) ③ `-notmatch` 로 remote 확인 — remote 2개 이상이면 오판 ④ 03 실행 중 병행 업로드 시 쓰다 만 JPEG 업로드 | `rclone lsd` 사전 확인 / `/` 로 교체 / `-notcontains` / `--min-age 1m` |
| `bev/` 패키지화 | `bev_render_worker` 가 Windows(spawn) 자식에서 **참조로 import** 되므로 저장소 루트가 `sys.path` 에 있어야 한다 | 노트북에 `sys.path` 가드. 전처리는 루트에서 `python -m bev.road_prep` |
| `refine_pose` 페널티 정규화 | `PEN_XY*((dx²+dy²)/DXY.max()²)` 인데 `dx²+dy²` 의 최대는 모서리에서 `2·DXY.max()²` 다. 모서리 페널티가 `PEN_XY` 의 **2배**가 되어 대각 보정만 부당하게 불리했다(등방이어야 한다) | 분모를 `2*DXY.max()**2` 로. **27% 의 파노라마에서 pose 가 바뀐다**(이동 변화 중앙 1.4m). **전량 관찰**: 페널티가 절반이 되면서 `\|dxy\| ≥ 8m` 가 5.6% → 10.8% 로 두 배 (X-22 §1). 수식은 옳아졌으나 계수 재조정이 필요할 수 있다 → §0.2-B |
| suptitle 조립 위치 | 호출부(노트북)에서 f-string 으로 만들다 보니 **검증한 문자열과 실제 문자열이 어긋났다** — 짧은 형식으로 폭을 재고 긴 형식을 넣어 G7 이 깨졌다 | `bev_core.suptitle_for()` 한 곳으로 일원화. 테스트와 실제가 같은 함수를 쓴다 |

# 부록 B. 폐기된 설계

| 대상 | 처분 |
|---|---|
| `_column_base_z`, `mask_to_footprint` | 삭제 (X-01) |
| `calibrate_depth_to_metric`, `footprint_from_zmap` | 삭제 (D-04 로 대체) |
| `raycast_shadow*`, `_get_raycast_geometry`, `_raycast_cache`, `l_vis_from_rays` | 삭제 (D-05 로 대체) |
| `split_vehicles_by_angle` | 삭제 (D-09) |
| `03_bev_shadow.ipynb` (CPU 판) | 삭제. GPU 판이 기준 — 분석·렌더를 GPU/CPU 로 병렬화해 훨씬 빠르다 |
| `A_TOTAL_DISK` 기반 `compute_dsi_refined` | `compute_dsi(road_occluded_frac)` 로 교체 (D-07). 원판 값은 `a_shadow_disk_m2` 로 참고 기록만 |
| 01 의 `GROUND_KEYWORDS` `"sky"`→skyscraper 충돌 | **01 재실행 금지.** 소스만 수정, 03 에서는 class_id 48 로 처리 (1,500표본 중 2건, 등급 영향 없음) |
